# Controls disguise behavior, presentation, bounds, storage, and diagnostics.
# Generated schema version 1.
# This file cannot grant permissions or execute commands.
schema_version = 1
module_id = "disguise"
documentation_version = 2

[module]
# Enables the module after dependency validation. `true` or `false` Apply class live.
enabled = true
# Defines truthful behavior while this module is disabled. `command_unavailable`, `provider_fallback`, `read_only` Apply class live.
disabled_behavior = "command_unavailable"

[gui]
# Selects enhanced GUI presentation without changing command access. `command_only`, `gui_preferred`, `inherit`, `off`, `on` Apply class live.
mode = "inherit"
# Allows eligible bare command routes to prefer a dedicated workflow. `true` or `false` Apply class live.
bare_command_opens = true
# Selects the stable default workflow page. 1 through 64 characters, pattern `[a-z0-9_]+` Apply class live.
default_page = "overview"

[limits]
# Bounds persisted records owned by this module. 1 through 100000 Apply class live.
maximum_records = 1000
# Bounds command and GUI result pages. 4 through 100 Apply class live.
maximum_page_size = 45

[rate]
# Bounds admission attempts independently of permission cooldown duration. 1 through 10000 Apply class live.
maximum_actions_per_minute = 30

[storage]
# Defines bounded data retention where the module owns persistent records. 0 through 3650 Apply class live.
retention_days = 30

[failure]
# Defines provider and validation failure behavior. `fail_closed`, `previous_known_good`, `read_only` Apply class live.
mode = "fail_closed"

[audit]
# Defines the minimum audit class for module actions. `admin_action`, `destructive`, `metadata_only`, `none` Apply class live.
class = "admin_action"
# Defines the module redaction class. `security_critical`, `sensitive`, `standard` Apply class live.
redaction = "standard"

[runtime]
# Enable allowlisted server authoritative disguise abilities `true` or `false` Apply class live.
disguise_abilities_enabled = false
# Allow the curated Blaze fireball to damage another player `true` or `false` Apply class live.
disguise_blaze_allow_pvp = false
# Fire duration applied by the curated Blaze fireball 0 through 60 Apply class live.
disguise_blaze_fire_seconds = 5
# Maximum direct damage for the curated Blaze fireball 0.0 through 100.0 Apply class live.
disguise_blaze_fireball_damage = 5.0
# Maximum travel range of the curated Blaze fireball 4 through 128 Apply class live.
disguise_blaze_maximum_range = 48
# Clear active disguises when the subject dies `true` or `false` Apply class live.
disguise_clear_on_death = true
# Clear active disguises when the subject disconnects `true` or `false` Apply class live.
disguise_clear_on_logout = true
# Maximum simultaneous active disguises 1 through 4096 Apply class live.
disguise_maximum_active = 256
# Enable viewer filtered disguise sound profiles `true` or `false` Apply class live.
disguise_sounds_enabled = true
# Enable allowlisted server authoritative disguise traits `true` or `false` Apply class live.
disguise_traits_enabled = false
# Enable bounded vanilla client mob proxy rendering `true` or `false` Apply class live.
disguise_vanilla_proxy_enabled = true
