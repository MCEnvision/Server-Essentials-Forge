# Repository map
CodeGraph reported an up-to-date index on 2026-09-24T20:06:46.273Z: 562 files, 20,027 nodes and 66,209 edges, including 550 Java files. Graph output established relationships; it did not prove runtime outcomes. Native calls, reflection, runtime interface dispatch, Gradle configuration and external evidence joins require direct bounded inspection.

| Boundary | Source and test locators | Remaining oracle |
| --- | --- | --- |
| Command admission and completion | `kernel/KernelCommandExecutor.java`, `kernel/policy/CommandExecutionService.java:63`, `kernel/command/CommandCatalog.java`; `KernelCommandExecutorCatalogTest`, `CommandExecutionLeaseTest`, command GameTests | Every canonical action and permitted alternate route observes current authority, exact domain effect and truthful terminal result. |
| Audit and observation | `audit/SecurityAuditService.java`, `audit/NativeAuditFileProvider.java:45`, `commandlog/FileLogSink.java`; `AuditRuntimeProbe`, `AuditServiceTest`, `CommandRedactionPolicyTest`, `FileLogSinkTest` | One correlated event reaches mandatory native sink; unsafe target fails closed; optional projections redact and cannot disable audit. |
| Persistence | `storage/repository/StorageCoordinator.java:54`, `storage/AtomicFileStore.java`, coalesced worker and domain repositories; `RepositoryPersistenceTest` and Phase002 owner matrix | Preserve valid data, explicit recovery, bounded flush and exact cross-store outcome through interruption/restart. |
| GUI authority | `gui/protocol/SefGuiServer.java:91`, `PanelActionValidator.java:11`, `SefSessionManager`, `SefNetwork`; protocol tests | Session/panel/entry revision and permission are rechecked on server before mutation. |
| GUI presentation | `gui/UniversalGuiCatalog.java:53`, client screens and HUD, command feedback | Actual laptop input/rendering/narration, responsive state, privacy and fallback equivalence. |
| Provider and diagnostics | `permissions/PermissionService.java`, LuckPerms bridge, `kernel/KernelCommands.java:60`, Fancy Tags/logging/module doctors | Exact authenticated login and permission projection; outage is fail closed; health reports distinguish provider, kernel and persistence failure. |
| Inventory and evidence | `src/test/java/com/enviouse/sef/docs/UniversalCommandMatrixGenerator.java` and its test, AuditInventoryGenerator family | Complete input joins, commit/artifact checks, deterministic counts, no missing action dimensions or false pass. |
| Build and supply | `build.gradle:192`, dependency manifest at lines405 onward and probe task539 onward, `gradle.properties` | JNA/JNA Platform5.14.0 compile-only, supplied through pinned platform, no duplicate runtime in JAR; exact advisory API/configuration dispositions. |

Paths without `src/` prefix in the table are relative to `src/main/java/com/enviouse/sef/`. Unit tests are under `src/test/java/com/enviouse/sef/`; GameTests live in corresponding main domain packages.

The command kernel joins source, actor, permissions, policies, cooldown, warmup, confirmation, cost and audit. GUI catalog maps only player-facing actions into categories and fallback routes. StorageCoordinator registers stores before start, detects recovery/unsupported/error states and owns scheduled and shutdown flush. NativeAuditFileProvider chooses Windows handles or POSIX descriptors; security evidence must retain opened-object identity, not only a checked path. The direct static graph cannot prove optional reflection or native ABI correctness.

Existing health controls should be extended only where a named remaining failure cannot otherwise be distinguished. Keep collection bounded, authorized, sanitized, default off where additional capture is introduced, and prove disabled cost and cleanup. The current source search found doctor registrations but no debug capture command registration.

