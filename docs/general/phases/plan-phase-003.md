# Phase 003 Execution Plan

> **Plan ID:** PLAN-PHASE-003  
> **Phase ID:** SEFAUD-PHASE-003  
> **Owner:** Command policy kernel  
> **Classification:** MANDATORY  
> **Master plan:** [plan.md](../plan.md)  
> **Phase sequence:** 003 of 007

## Purpose and Ownership

This active phase closes the executable administrator-action contract. It retains compatible Phase 000 through Phase 002 proof, but it must reconcile the conflicting task 310 and task 317 inputs before selecting any remaining action proof. The master owns requirements, decisions, interface signatures, acceptance criteria, and topology. This blueprint owns only the detailed Phase 003 execution of SEFAUD-REQ-004 and the current prerequisite deltas it exposes.

P003-TASK-010 is the next executable action. It first performs a read-only reconciliation of the task 310 and task 317 candidate inputs. It then delivers the shared capture contract SEFAUD-IF-004 before capture-dependent runtime collection and repairs the observed command-tree and provider-fixture failures without granting blanket operator authority or weakening authentication. Parse, callback, registration, catalog, and visibility results remain discovery evidence rather than an action pass.

## Evidence-Based Entry State

| Evidence class | Area | Finding | Source or command | Freshness condition |
|---|---|---|---|---|
| OBSERVED | Current task | P003-TASK-010 is next. Historical P003-TASK-001 through P003-TASK-009 retain only input-compatible proof. | master Sections 4a, 11, 12, 13, 19 | Candidate, generator, fixture, dependency, or artifact change. |
| OBSERVED | Matrix conflict | Task 310 reports 729 rows with 324 partial and 389 open executable rows. Task 317 reports 713 open, 16 partial, and zero passed. | SRC-012, FIND-003 | Candidate-bound input or generator semantics change. |
| OBSERVED | Authorization fixture | A protected doctor command was denied in the client tree before transmission. LuckPerms NeoForge 5.4.140 on NeoForge 21.1.235 failed login with capability initialization and invalid player-data errors. | SRC-012, FIND-004 | Provider artifact, lifecycle, login fixture, or permission projection change. |
| OBSERVED | Diagnostics gap | Doctors exist but SEFAUD-IF-004 capture controls do not. | SRC-005, FIND-009 | Command registration, capture implementation, or support documentation change. |
| OBSERVED | Native and dependency proof | Linux native writer and JNA supply packets are compatible only after current source, artifact, ABI, and fixture comparison. | SRC-010, SRC-012, FIND-006 | Native provider, JAR, dependency graph, runtime, or sink change. |

## Scope Boundaries

### Included Scope

- SEFAUD-REQ-004 closes each executable administrator action through authority, source, target, argument, effect, failure, durable result, restart, feedback, audit, and equivalent-route dimensions.
- SEFAUD-REQ-004 delivers SEFAUD-IF-004 before dependent capture evidence, including default-off authorization, bounded capture, redaction, reset, and support documentation.
- SEFAUD-REQ-004 preserves negative behavior for admin_journal, afk_zones, approvals, capability_leases, chat_channels, display_ownership, display_profiles, player_warp_review, portal_policy, resource_governor, resource_worlds, rollouts, server_presentation, spawn_ecology, staff_duty, waypoints.
- SEFAUD-REQ-002, SEFAUD-REQ-003, SEFAUD-REQ-006, SEFAUD-REQ-007 prerequisite deltas discovered by current command, provider, audit, storage, native, or advisory work are repaired in this contiguous phase and rerun through their affected command evidence.

### Explicit Exclusions

- FUT-001, NG-001 exclude implementing any unavailable-family runtime.
- FUT-002, FUT-003 exclude unrelated commands, UI features, integrations, and broad command-framework replacement.
- NG-002 preserves Minecraft 1.21.1, NeoForge 21.1.235, Java 21, current mappings, wrapper, loader, and protocol boundaries.
- Phase 004 owns full UI accessibility and visual polish. Phase 005 owns full lifecycle convergence. Phase 006 owns final clean candidate regression. Phase 007 owns final release readiness and integration.

## Phase Contract

### SEFAUD-PHASE-003 — Administrator Command Behavioral Closure

**Objective:** Reconcile the real command matrix inputs, deliver bounded capture, diagnose authentic authority and provider fixtures, and close every executable action and unavailable family with truthful effect, durable-result, feedback, audit, and route evidence at one candidate revision.  
**Owner:** Command policy kernel  
**Dependencies:** SEFAUD-PHASE-002, SEFAUD-REQ-001, SEFAUD-REQ-002, SEFAUD-REQ-003, SEFAUD-REQ-006, DEC-002, DEC-009, EXT-001, EXT-002  
**Canonical requirements:** SEFAUD-REQ-004  
**Documentation and release impact:** Update README.md, DOCUMENTATION.md, docs/COMMAND_REFERENCE.md, docs/PERMISSION_REFERENCE.md, docs/SEF2_ACCEPTANCE.md, docs/SECURITY_REVIEW.md, docs/COMPATIBILITY_MATRIX.md, docs/MIGRATION_GUIDE.md when behavior changes, test.md, docs/README.md when paths change. Generated references are regenerated. No publication occurs.  
**Next transition:** SEFAUD-PHASE-004  

**Entry criteria**

- SEFAUD-REQ-004 retains signed Phase 000 through Phase 002 history and candidate provenance.
- SEFAUD-REQ-004 has task 310, task 317, generator, artifact, fixture, and dependency inputs available for reconciliation.
- SEFAUD-REQ-004 has a disposable node-1 dedicated-server plan and an exact cleanup registry before any runtime work.

**Implementation scope**

- SEFAUD-REQ-004 implements SEFAUD-IF-004 under P003-TASK-010 before collecting dependent evidence.
- SEFAUD-REQ-004 closes only evidence-backed command, authorization, provider, effect, persistence, feedback, audit, redaction, native, and route defects.
- SEFAUD-REQ-004 records unavailable families as negatively tested unavailable contracts, never new functionality.

**Execution order**

1. P003-TASK-010 executes SEFAUD-REQ-004 read-only task 310 and task 317 reconciliation, SEFAUD-IF-004 delivery, authentic authority and provider diagnosis, then feedback, audit, and sink prerequisites.
2. P003-TASK-011 executes SEFAUD-REQ-004 unavailable-family negative closure using the delivered capture.
3. P003-TASK-012 executes SEFAUD-REQ-004 catalog-wide real-path matrix collection after prerequisite repairs.
4. P003-TASK-013 executes SEFAUD-REQ-004 focused remediation and invalidated-row reruns.
5. P003-TASK-014 executes SEFAUD-REQ-004 performance, reference, documentation, and evidence reconciliation.
6. P003-TASK-015 executes SEFAUD-REQ-004 phase-wide clean verification and sequential integration closure.

**Required evidence**

- SEFAUD-REQ-004 has input-level reconciliation for both historical matrix records and every current action dimension.
- SEFAUD-REQ-004 has authentic console, provider, denial, grant, revocation, refresh, reconnect, outage, effect, restart, route, feedback, and audit proof at real entry points.
- SEFAUD-REQ-004 has native Linux source, ABI, artifact, runtime, opened-object, and JAR supply evidence under DEC-009, with foreign paths analysed but not invented as runtime prerequisites.

**Exit criteria**

- SEFAUD-REQ-004 meets SEFAUD-AC-010, SEFAUD-AC-011, SEFAUD-AC-012 and provides a complete SEFAUD-IF-006 packet.
- SEFAUD-REQ-004 contains no row labeled passed from parse, callback, registration, or unrelated shared proof alone.
- No known mandatory phase-owned defect remains.

## Inputs and Upstream Contracts

| Input or contract | Provider | Required state | Validation | Failure behavior |
|---|---|---|---|---|
| SEFAUD-IF-001 | Phase 000 | Candidate, surface, dimension, evidence, disposition fields preserve compatible task 310 inputs. | Reconcile both historical inputs and current generator inputs. | Reject missing, duplicate, stale, or dropped input. |
| SEFAUD-IF-002 | Phase 001 | Current authority snapshot reaches mutation and terminal outcome. | Grant, deny, revoke, refresh, reconnect, outage fixtures. | Fail closed without protected mutation. |
| SEFAUD-IF-003 | Phase 002 | Store outcome distinguishes not_committed, committed, unknown. | Before and after state, receipt, restart, recovery. | Resolve receipt before retry. |
| SEFAUD-IF-005 | Phase 001 | Opened native object and supplied JNA identity remain valid. | ABI, artifact, Linux runtime, link, swap, flush tests. | Fail closed and preserve prior valid state. |

## Outputs and Downstream Contracts

| Output or contract | Consumer | Guaranteed state | Compatibility or versioning | Evidence |
|---|---|---|---|---|
| SEFAUD-IF-004 | Remaining Phase 003, Phases 004 through 007 | Authorized, default-off, bounded, reset-safe capture. | Event schema version 2. | Self-tests, status record, sanitized support packet. |
| Complete command matrix | Phases 004 through 007 | Each executable action has independent dimensions and invalidation links. | SEFAUD-IF-001 dispositions remain distinct. | Candidate-bound matrix and join ledger. |
| Command evidence packet | Phase 004 | Known client residual claims are explicit, no unsupported host claim. | DEC-009 portable client contract. | Linux server proof and scoped laptop claim records. |

## Work Packages

| Task ID | Requirement IDs | Work | Inputs and dependencies | Outputs | Affected components or interfaces | Verification |
|---|---|---|---|---|---|---|
| P003-TASK-001 | SEFAUD-REQ-004 | Revalidate entry evidence and freeze live executable action inventory from dispatcher, catalog, shortcuts, modules, automation, controls, GUI descriptors, and owners. | Retained Phase 000 through Phase 002 proof, DEC-009, EXT-001, EXT-002. | Stable executable and unavailable inventory. | CommandRegistrationHandler, KernelServices, CommandCatalog, ShortcutRegistry. | Reject missing, duplicate, collision, stale, disabled, artifact-mismatch, and unowned rows. |
| P003-TASK-002 | SEFAUD-REQ-004 | Define the universal action row with owner, route, authority, source, target, input, effect, failure, store, feedback, audit, redaction, native, and invalidation fields. | P003-TASK-001, SEFAUD-IF-001, SEFAUD-IF-002, SEFAUD-IF-003, SEFAUD-IF-005. | Machine-validated dimension and join schema. | CommandDefinition, matrix generator, audit and storage contracts. | Reject parser-only pass, missing oracle, missing writer, and unsupported not_applicable disposition. |
| P003-TASK-003 | SEFAUD-REQ-004 | Prove registration, root ownership, discovery, module state, shortcut activation, collision policy, and parser structure without treating them as behavioral completion. | P003-TASK-001, P003-TASK-002. | Discovery ledger marked nonclosing for effect dimensions. | Dispatcher, registrars, shortcut registry. | Live tree, suggestion, module, and collision fixtures report exact ownership. |
| P003-TASK-004 | SEFAUD-REQ-004 | Exercise absent, deny, authentic grant, inherited, revoke, refresh, reload, reconnect, hierarchy, exemption, feature, quota, cooldown, cost, warmup, and confirmation states. | P003-TASK-002, SEFAUD-IF-002. | Authority matrix with immutable denied state. | PermissionService, provider bridge, policy services. | Provider-only and matching SEF fixtures distinguish lifecycle, login, projection, and command denial. |
| P003-TASK-005 | SEFAUD-REQ-004 | Exercise allowed sources and reject undeclared sources, then test self, online, offline, equal, higher, exempt, vanished, unknown, ambiguous, stale, and bulk targets. | P003-TASK-002, P003-TASK-004. | Source and target evidence rows. | Brigadier sources, target policies, visibility and offline adapters. | Before and after state plus disclosure-safe suggestions and errors. |
| P003-TASK-006 | SEFAUD-REQ-004 | Exercise missing, valid, boundary, malformed, namespaced, unknown registry, long, quoted, Unicode, control, selector, separator, and injection-like arguments. | P003-TASK-002. | Normalized argument corpus and feedback outcomes. | Brigadier arguments, AliasCompiler, validators, component formatters. | Real valid and rejected execution against domain oracle with bounded output. |
| P003-TASK-007 | SEFAUD-REQ-004 | Prove preview and confirmation bind actor, source, action, arguments, target, policy, provider, configuration, tree, profile, session, revision, and expiry. | P003-TASK-003, P003-TASK-004, P003-TASK-005, P003-TASK-006. | Replay, stale, switch, refresh, reload, reconnect, and race rejections. | ConfirmationService, CommandExecutionService, panel and automation owners. | Unchanged state and receipt on every rejected stimulus. |
| P003-TASK-008 | SEFAUD-REQ-004 | Prove each action exact-once effect, validation before mutation, failure cut point, cost, cooldown, durable result, restart, recovery, and concurrency behavior. | P003-TASK-004, P003-TASK-005, P003-TASK-006, P003-TASK-007, SEFAUD-IF-003. | Action-specific semantic state and durable ledger. | KernelCommandExecutor, domain owners, repositories. | State hashes, receipts, fault injection, bounded wait, restart and recovery oracle. |
| P003-TASK-009 | SEFAUD-REQ-004 | Prove canonical, shortcut, GUI, fallback, alias, bundle, sudo, panel, profile, integration, and legacy routes preserve canonical authority and outcome. | P003-TASK-002, P003-TASK-004, P003-TASK-008. | Route equivalence ledger. | Shortcut, alias, bundle, sudo, panel, profile, GUI services. | Correlation, effect, cooldown, store, feedback, and audit equality with nested denial. |
| P003-TASK-010 | SEFAUD-REQ-004 | First reconcile task 310 and task 317 read-only inputs and generator semantics. Then deliver SEFAUD-IF-004, diagnose exact NeoForge 21.1.235 and LuckPerms 5.4.140 login and projection failures, and prove feedback classes, attribution, audit, redaction, native sink, and dependency joins. | SEFAUD-IF-001, SEFAUD-IF-002, SEFAUD-IF-003, SEFAUD-IF-005, EXT-001, EXT-002. | Reconciled matrix delta, capture version 2, provider diagnosis, feedback and native audit ledger. | KernelCommands, PermissionService, AuditService, CommandRedactionPolicy, NativeAuditFileProvider, Gradle graph. | Read-only candidate join first. Then enable, status, off, exact bounds, privacy, reset, console and default-denied self-tests, authentic provider lifecycle, writer and JAR inspection. |
| P003-TASK-011 | SEFAUD-REQ-004 | Prove all sixteen unavailable families reject absent, denied, granted, bypass, console, GUI, fallback, preview, generic transition, alias, and bundle attempts without mutation or capability claim. | P003-TASK-001, P003-TASK-010. | Sixteen negative rows and diagnostic dispositions. | ServerControlCatalog, schemas, execution service, panel descriptors, docs. | Each family has entry point, unchanged state, terminal result, audit rule, and recovery cleanup. |
| P003-TASK-012 | SEFAUD-REQ-004 | Run catalog-wide dispatcher, policy, domain, GameTest, dedicated-server, and multi-actor real-path workflows and record every open, partial, failed, passed, and not_applicable row. | P003-TASK-010, P003-TASK-011. | Complete initial matrix with no hidden evidence loss. | Test source sets, GameTests, configured server tasks, command owners. | Real effect, failure, store, restart, feedback, audit, and route dimensions reject lower-fidelity substitution. |
| P003-TASK-013 | SEFAUD-REQ-004 | Reproduce and repair confirmed defects in dependency order, preserve compatible interfaces, add real-boundary regression, and rerun direct and transitive invalidation. | P003-TASK-012 findings. | Closed finding ledger and renewed affected proof. | Only implicated command, policy, domain, storage, feedback, audit, redaction, native, provider components. | Failure before repair when practicable, focused regression, blast-radius rerun, clean recovery. |
| P003-TASK-014 | SEFAUD-REQ-004 | Measure catalog-scale discovery, suggestions, bulk, bundles, schedules, actors, refresh, audit volume, and valid input limits. Regenerate references and reconcile documentation and evidence. | P003-TASK-013 final implementation revision. | Performance record, zero-drift references, support documentation, sanitized evidence index. | Dispatcher, policy caches, audit queue, generators, tracked docs. | Fixed baseline, declared limits, two generation runs, no unbounded queue, scan, output, or retained session. |
| P003-TASK-015 | SEFAUD-REQ-004 | Run phase-wide clean verification after the last change, package the SEFAUD-IF-006 receipt, integrate sequentially, verify resulting candidate, and sign the phase tag. | P003-TASK-014, SEFAUD-IF-006. | Merged phase packet and next-phase entry evidence. | Clean checkout, artifact, CI, GitHub phase workflow. | Exact commands, result reports, PR checks, independent review, merge commit, tag signature, cleanup verification. |

## Domain Recipes for P003-TASK-008 and P003-TASK-012

The universal matrix retains one row for every executable action. This table binds those rows to existing source workflows instead of deriving effect, failure, or recovery oracles during execution. Every recipe invokes its documented canonical command, enhanced route, or server service entry, captures a before state and a terminal correlation, and joins one feedback result, one terminal audit event, and the declared durable outcome. A row waits only for its recorded configured warmup, confirmation, cooldown, queue, or deadline policy. If the action has no asynchronous policy, it waits for the deterministic terminal receipt and one server tick. A timeout is classified as missing terminal event, policy expiry, queue failure, or worker failure and never as an expected domain failure without its stated oracle.

| Executable family | Source workflow | Actual entry and synthetic fixture | Exact success, negative, and recovery invariant | Bounded wait source | Evidence join |
|---|---|---|---|---|---|
| Catalog, permissions, cooldowns, warmups, confirmations, costs | test.md §§352, 561, 602, 2564. Command authorization, canonical pipeline, cooldowns warmups confirmations and costs, permission-derived cooldowns. | Run each catalog action through its canonical dispatcher root with synthetic owner, seniorstaff, juniorstaff, playera, and playerb. Use exact grant, wildcard, exact deny, revocation during active lease, changed confirmation, and provider outage fixtures. | An allowed action reaches exactly one declared effect. A denied, stale, revoked, malformed, or expired request changes no protected state, cost, quota, cooldown, or store. Cancellation refunds only the documented reservation and a reused confirmation never dispatches. | Recorded action warmup, confirmation expiry, cooldown, or provider deadline. Otherwise terminal receipt plus one server tick. | SEFAUD-IF-002 decision and reason, before and after semantic state, cost and cooldown lease, SEFAUD-IF-003 receipt, feedback key, and one terminal audit id. |
| Homes, teleport requests, direct teleport, back, spawn, warps, player warps, RTP | test.md §§694, 753, 789. Homes, teleport requests, direct teleport back spawn and warps. | Use `/sethome`, `/home`, `/homeadmin`, `/tpa`, `/tpaccept`, `/tpdeny`, `/tpcancel`, `/tp`, `/back`, `/spawn`, `/warp`, `/pwarp`, and `/rtp` against online, offline where supported, vanished, higher-rank, stale, unsafe, and cross-dimension synthetic targets. | Safe arrival is within policy. Deleted, stale, unsafe, blocked, expired, or losing request paths move no player and preserve histories. Opposed accept, deny, and cancel races select one terminal request state. Failed safe-search leaves position unchanged. | Recorded warmup, request expiry, chunk-search budget, or one terminal teleport result and one server tick. | Position and history before and after, request or home revision, feedback, cooldown and cost result, durable receipt, route correlation, terminal audit. |
| Private messaging, social spy, mail, nicknames, connection output, reminders, custom text | test.md §§828, 849, 866, 883. Private messaging and ignore, social spy, mail, identity connection messages reminders and custom text. | Use `/msg`, `/tell`, `/w`, `/r`, `/ignore`, `/socialspy`, discovered `/mail` subcommands, nickname and reminder commands with online, vanished, ignored, renamed, offline, ambiguous, expired, and reconnect fixtures. | Each eligible event delivers once to authorized recipients. Denied, ignored, vanished, expired, or failed-write paths create no phantom mail, reminder, reply target, or disclosure. Message bodies, private markers, and hidden identities never enter ordinary audit or observers. | Recorded mail retention, reminder interval, or acknowledgement maximum. Otherwise terminal delivery or receipt plus one server tick. | UUID owner and mailbox state, audience projection, redacted feedback and audit, persistence receipt, reconnect and restart result. |
| Moderation, punishment, persistent controls, command spy, optional file logging | test.md §§902, 950, 983, 1000. Warning mute ban and kick matrix, persistent controls, command spy, optional file logging. | Use `/warn`, `/mute`, `/ban`, `/tempban`, `/pardon`, `/kick`, `/kickall`, jail, freeze, inventory lock, building disablement, banned-item, command spy, and discovered logging commands with self, equal, higher, exempt, vanished, offline, duplicate, and conflicting-expiry fixtures. | Restart before expiry retains punishment and control. Restart after expiry clears it exactly once. Duplicate or conflicting punishments resolve by published revision without duplicate enforcement. Vanilla ban lists remain authoritative where specified. Kickall preview, cap, confirmation, changed online cohort, protected exclusion, and replay rejection are exact. Corrupt moderation data recovers fail closed. Logging unsafe link, disk, queue, rotation, and restart failures never claim success. | Punishment expiry or logging retention deadline when configured, otherwise deterministic enforcement event, terminal record, and one server tick. | Moderation repository state and vanilla ban state, cohort before and after, enforcement packet outcome, redacted feedback, audit, native writer object and flush result, restart receipt. |
| Kits, inventory, item utilities, player utilities, gamemode, workstations, super enchanting | test.md §§1047, 1064, 1116, 1158. Kits, inventory and item utilities, player utilities, gamemode workstations and super enchanting. | Use `/kit`, `/clearinventory`, `/invsee`, `/disposal`, `/i`, `/feed`, `/heal`, `/fly`, `/god`, `/gm`, workstation roots, `/enchant`, `/sef enchant`, `/superenchantingtable`, and `/set` with exact stacks, slot components, stale menus, cursor item, target disconnect, permission loss, registry change, and collision fixtures. | Successful kit claim, give, move, enchant, and workstation effect occurs once. Admission failure preserves exact slots, components, claim history, and viewer or target state. Stale or revoked InvSee click commits no mutation. Feed leaves food 20, saturation 0.0F, and prior health exactly. Shortcuts never weaken canonical policy. | Menu confirmation or configured cooldown if present, otherwise server inventory acknowledgement and one tick. | Exact slot and component comparison, player utility state, menu revision, feedback, durable receipt, canonical action correlation, terminal audit. |
| Native economy, ledger, worth, sale, command costs, signs, import | test.md §§1201, 1213, 1248, 1266, 1305. Provider ownership, money and ledger, worth sale and command costs, economy signs, import and performance. | Use `/balance`, `/bal`, `/pay`, `/eco`, `/worth`, `/sell`, economy sign roots, and import controls in native, external, disabled, and import_once modes with exact minor-unit amounts, account limits, offline and hierarchy targets, quote changes, external provider failure, and uncommitted cost-hold crash fixtures. | Arithmetic is exact minor units. Identical idempotency key creates at most one matching mutation. The same key with changed account, amount, currency, reason, or metadata is refused without account, ledger, cache revision, or hold change. Failed sale restores exact slots and components and credits zero. Successful sale removes eligible item once and posts one exact credit. Restart after uncommitted cost hold yields exactly one refund. Disabled or failed provider exposes no mutating route or charge. | Recorded confirmation, sign interaction, import, or provider deadline. Crash recovery waits for restart completion and one receipt settlement, never a wall-clock guess. | Account, ledger, hold, item slots and components, provider mode, idempotency key, receipt, feedback, audit, and restart state. |
| GUI protocol, typed workflows, offline give, panels, batch and participant execution | test.md §§1323, 1442, 1587, 1613, 1633. Client protocol and GUI pilot, universal GUI coverage, offline give queue, panel drafts and publication, batch and participant execution. | Use real command fallback, enhanced route, incompatible protocol fixture, no-SEF fallback, forged session, stale revision, player and item picker, offline give, panel draft, and paced cohort entries. | Invalid payload rejects before mutation. GUI and command produce same action and audit id. A queued give revalidates at execution and grants once after reconnect or restart. Stale panel publication conflicts rather than losing updates. Frozen cohort and participant authority remain separate. | Negotiated session, queue, pacing, or seven-day offline-action expiry when declared. Otherwise validated server submission and one tick. | Session UUID and revision, protocol decision, UUID-bound queue record, panel revision, target cohort, before and after state, feedback, audit, receipt. |
| Aliases, bundles, profiles, fake identity, sudo, run, silent | test.md §§1651, 1690, 1727, 1754, 1784, 1825. Alias lifecycle, bundle lifecycle, command profiles, fake identity, sudo, run and silent. | Use `/sef alias`, `/sef bundle`, `/sef profile`, fake-action roots, `/sudo`, `/run`, and `/silent` with typed bindings, cycles, collisions, draft and published revisions, disconnect, stop, recovery, changed nested command, and one exact reviewed sudo grant. | Stored text never creates authority. Alias and bundle reject recursive, unsafe, or stale paths. Every bundle step revalidates and committed effects are not duplicated during recovery. Fake output changes no authenticated state. Sudo false uses target authority and true consumes one exact grant before dispatch. Run and silent deny nested privileged escape and retain truthful feedback and audit. | Published delay, bundle deadline, schedule expiry, consent expiry, or one terminal step receipt. | Published revision, binding and target cohort, effective and real actor, grant-consumption record, step receipt, feedback, audit, recovery result. |
| Fancy Tags and disguise | test.md §§1853, 2043. Fancy Tags, disguise. | Use discovered tag studio, record, assignment, import, export, transfer, backup, and disguise status, mob, trait, ability, enhanced and vanilla projection entries with module off, stale revision, vanished actor, permission loss, reconnect, and corrupted record fixtures. | Module and permission loss remove unauthorized command, projection, cache, and edit authority. Typed import and export reject unsafe content. Tag and disguise state remains revisioned, privacy-filtered, and restores or fails safely through restart. No client projection grants server mutation. | Published revision, transfer, animation or cache deadline where configured, otherwise authoritative state acknowledgement and one tick. | Module snapshot, record and assignment revision, projection state, sanitized feedback, audit, persistence and recovery receipt. |
| Supported server-control families and remaining systems | test.md §§2144, 2191, 2206, 2220, 2233, 2312, 2406. Common control workflow, operational safety, community and staff, onboarding and rewards, recovery and world operations, chat safety admission and access, diagnostics data packs and verified recovery. | Use each enabled catalog control through its command, preview, confirmation, execute, status, and recovery route with current revision, synthetic participant, module disablement, provider absence, restart, and concurrent policy-change fixtures. | Each supported control follows its published preview and revision contract, preserves protected authority and durable state on denial, and either commits once with exact recovery material or reports explicit uncommitted or unknown state. Admission queue remains bounded, FIFO, and releases or removes every negotiation future. | Declared control confirmation, schedule, queue, maximum_wait_seconds, reload, or recovery deadline; otherwise terminal server event and one tick. | Control revision, participant and cohort state, queue or recovery record, feedback, audit, durable receipt. |
| Modular configuration and command-exposed configuration | test.md §§2621, 2711, 2742, 2824. Module inventory, transactional editing and reload, migration backup and rollback, module enabled-state consistency. | Use `/sef config status`, `modules`, `inspect`, `explain`, `validate`, `set`, `reload`, `rollback`, and migration roots with bounded module files, min, max, wrong type, invalid enum, stale revision, mixed valid and invalid change, corrupted copy, and restart fixtures. | Validation and publication are all-or-nothing. Invalid input retains prior known-good revision. A stale migration or edit commits nothing. Backup, journal, rollback, and recovery preserve exact previous state. Module enabled state agrees across direct command, catalog, GUI, and doctor. | Published reload target, watcher debounce, migration revision, or deterministic publication event plus one tick. | Configuration revision, module snapshot, backup and journal identity, command-tree and GUI projection, feedback, audit, durable recovery result. |

The sixteen unavailable families admin_journal, afk_zones, approvals, capability_leases, chat_channels, display_ownership, display_profiles, player_warp_review, portal_policy, resource_governor, resource_worlds, rollouts, server_presentation, spawn_ecology, staff_duty, waypoints remain P003-TASK-011 negative-only contracts. They never use a success recipe, gain a handler, or become a supported control family.

## Architecture and Implementation Boundaries

SEFAUD-IF-002 carries canonical action, route and actor context from admission through mutation and terminal audit. SEFAUD-IF-003 owns durable outcome and blocks unsafe retry of unknown state. P003-TASK-010 adds SEFAUD-IF-004 once, without altering historical phases. Capture observes only sanitized event data and cannot grant authority, alter an action, retain a secret, or re-enable after reset. Audit remains mandatory and separate from optional diagnostic output.

The native writer remains owned by SecurityAuditService and NativeAuditFileProvider. Review actual POSIX and Windows source branches, JNA API and ABI types, calling conventions, error paths, object identity, Gradle resolution, packaged JAR entries, and NeoForge runtime supply. Execute available Linux fixtures only. Record macOS and Windows as source and artifact analysis or compatible retained proof where applicable, never as missing foreign-client or native-runtime prerequisites.

## Failure, Recovery, and Edge Cases

| Scenario | Detection | Required behavior | Recovery or rollback | Regression proof |
|---|---|---|---|---|
| Matrix input disagreement | Read-only count, row identity, candidate, generator, source-locator, and evidence-join comparison completes before capture exists. | Preserve compatible task 310 records, explain task 317 semantics, and leave unmatched rows open. | Repair generator or joins, regenerate without silently dropping records. | P003-TASK-010 input manifest and deterministic reconciliation report. |
| Provider login and doctor projection | Provider-only then SEF fixture records capability stage, synthetic identity validity, lifecycle order, tree visibility, and server decision within 60 seconds after ready state. | No command grant occurs until the authentic provider path is healthy. The observed doctor denial is tested with the exact dispatcher-discovered doctor permission only, not operator status. | Repair fixture or confirmed component, reauthenticate, refresh tree, and recheck exact grant then exact deny. | P003-TASK-010 comparison with timeout classified provider startup, identity, lifecycle, projection, or policy. |
| Denied or revoked action | Synthetic playera starts with root discovery and one exact action grant, then exact deny or revocation immediately before KernelCommandExecutor mutation. | Protected domain state, cost, quota, cooldown, and store hash are unchanged; one redacted terminal audit and denial feedback occur. | Restore the exact fixture grant only after recording denial, then create a new request. | P003-TASK-004 and P003-TASK-012 real dispatcher action fixture with one server tick bounded wait. |
| Action effect, store, and restart | Teleport or home, moderation, economy, inventory, and server-control action fixtures use their canonical command entry and record before and after semantic hashes, operation receipt, feedback, and audit id. | Each action changes its declared state once. Domain failure reports not_committed, committed, or unknown truthfully and never borrows a callback result. | Resolve receipt before retry, restore synthetic state from validated recovery material, then rerun same route. | P003-TASK-008, P003-TASK-009, P003-TASK-012 fixtures with configured action timeout or 60-second classification. |
| Native unsafe object, flush, rotation, or restart | Linux NativeAuditFileProvider fixture creates owned safe file, unsafe link or reparse, object swap, metadata-unavailable, write, flush, rotation, queue failure, and restart cases. | Same opened object is checked, appended, and flushed. Unsafe or unavailable identity fails closed and prior valid content remains. | Quarantine only owned object, restore synthetic valid content, stop queue, and rerun after repair. | P003-TASK-010 source, ABI, artifact, JAR, and Linux runtime record with 60-second writer timeout classification. |
| Unavailable family transition | Each of the sixteen named families receives console, exact grant, explicit deny, bypass, preview, GUI fallback, alias, and bundle stimuli against its real catalog or generic transition entry. | No handler, ready preview, resolution, mutation, durable record, or capability claim appears. | Stop fixture, retain only redacted terminal evidence, and remove owned state. | P003-TASK-011 family rows with one server tick bounded negative oracle. |
| Capture saturation or reset | Output, accepted, dropped, truncated, expiry, target removal, reload, shutdown, and restart status fields are observed. | Saturation drops and counts instead of disabling. Timeout and lifecycle reset stop capture and never resume it. | Off is idempotent, status reports stop reason, and only owned capture files are removed after final use. | P003-TASK-010 60-second default, 300-second maximum, 1000 default, 10000 maximum, 4 MiB, 100 per second, 20 per tick, 1 ms per tick tests. |

P003-TASK-012 uses node-1 only for no-GUI dedicated-server, unit, and task-graph-verified headless GameTest fixtures. It creates synthetic owner, seniorstaff, juniorstaff, playera, and playerb identities, runs the actual dispatcher entry rather than a callback, and records the entry point, initial state, expected state, failure oracle, bounded wait, diagnostic cause, recovery action, and cleanup target in each row. Laptop work is limited to residual client command-tree, feedback, or GUI-route claims after the exact server join is confirmed.

## Diagnostics and Debugging

**Requirement IDs:** SEFAUD-REQ-004, SEFAUD-REQ-002, SEFAUD-REQ-003, SEFAUD-REQ-006, SEFAUD-REQ-007  
**Task IDs:** P003-TASK-010, P003-TASK-011, P003-TASK-012, P003-TASK-013, P003-TASK-014, P003-TASK-015  
**Controls:** P003-TASK-010 delivers `/sef debug on <category> <target> <seconds> <sample_limit>`, `/sef debug status`, `/sef debug off`; console uses `sef debug` after dispatcher discovery, player use needs explicit default-denied `sef.commands.sef.debug`, category is command, security, dependency, storage, gui, network, lifecycle, performance, target is action:<canonical-id>, actor:<synthetic-or-authorized-uuid>, or component:<registered-id>, and off is idempotent.  
**Signals:** Schema version 2 fields are capture_id, side, category, sanitized_target, accepted, dropped, truncated, remaining_ms, remaining_events, remaining_bytes, output, stop_reason, sequence, correlation_id, action, source, actor, effective_actor, decision, reason, expected_revision, observed_revision, duration_ms, queue_depth, and typed subsystem fields. Local action_id maps to canonical action, logical_side maps to side, sample_count maps to accepted, dropped_count maps to dropped, expected_state maps to typed expected state, actual_state maps to typed observed state, feedback_key, audit_id, writer_id, provider_stage, durable_outcome, and output_bytes are typed subsystem fields.  
**Collection procedure:** Follow the numbered procedure below, retain only sanitized candidate-bound evidence, and verify disable plus cleanup.  
**Headless verification:** node-1 uses the real console dispatcher, synthetic multi-actor server fixture, and only configured task graphs proven to start no client, renderer, or display; it proves server decision, mutation, durability, audit, and capture behavior.  
**Client verification:** The laptop is required only for residual client-tree rejection before transmission, command feedback receipt, and client route input after an exact private dedicated-server endpoint and joined player are verified; server results never close those claims.  
**Client audio isolation:** At prelaunch set the isolated pinned-version instance master audio to zero before launch. After launch use `hyprctl clients -j` to bind the exact owned Hyprland window, PID, and process tree to only its PipeWire or PulseAudio application stream, mute that owned stream with `wpctl` or `pactl`, and verify the matched stream reports muted before actions. Reapply mute and verify after resource reload, device change, reconnect, restart, or stream recreation. Stop the owned client if identity or mute cannot be verified. Cleanup stops any watcher, removes isolated audio state, and confirms the owned playback stream disappeared.  
**Budgets and privacy:** Capture is default false, one active server capture, 60 seconds default, 300 seconds maximum, 1000 events default, 10000 events maximum, 4 MiB sanitized output maximum, 100 accepted events per second maximum, 20 accepted events per tick maximum, and 1 ms aggregate collection per tick maximum. It rejects invalid, busy, missing-target, unsafe-sink, and unauthorized requests. Disabled mode performs no event allocation or file or network I/O. Enabled mode lazily formats redacted immutable snapshots into a bounded queue, drops and counts saturation without disabling, never reads live game state off-thread, and resets on timeout, reload, disconnect, target removal, shutdown, and restart. Optional sink failure stops optional capture with safe feedback while mandatory audit remains separately fail closed.  
**Regression and support:** P003-TASK-010 self-tests enable, status, disable, denial, absent target, expiry, reset, redaction, saturation, disabled overhead, enabled bound, and unchanged action outcome. DOCUMENTATION.md receives the enable, reproduce, disable, locate, redact, and submit procedure; the completion packet stores the minimum sanitized evidence.  

| Signal | Source and unit | Expected observation |
|---|---|---|
| decision, reason, expected_revision, observed_revision | Server authority boundary, enum and revision. | Grant mutates once, deny leaves state unchanged, provider failure fails closed. |
| capture_id, accepted, dropped, truncated, remaining_bytes | SEFAUD-IF-004 server capture, count and bytes. | Status matches bounded capture, saturation increments dropped, disabled output is absent. |
| side, action, source, provider_stage, feedback_key | Canonical event fields plus typed provider and feedback fields, enum. | Distinguishes login fixture, tree projection, policy denial, and domain failure. |
| audit_id, writer_id, durable_outcome | Typed audit and storage fields, stable id and enum. | One terminal event joins correct writer and declared durable state. |

CaptureStatus is schema_version:int, enabled:boolean default false, capture_id:opaque string absent while off, side:enum, category:enum, sanitized_target:string, remaining_ms:nonnegative long, accepted/dropped/truncated:nonnegative counters, remaining_events:int, remaining_bytes:long, output:owned relative locator, and stop_reason:enum. The schema uses category as the event type, with command, security, dependency, storage, gui, network, lifecycle, and performance events. Each event carries schema_version, capture_id, side, category, sanitized_target, sequence, timestamp, optional tick, correlation_id, component, action, source, pseudonymous actor/effective_actor, decision, reason, expected/observed revision, duration_ms, and only applicable typed subsystem fields. Absence means unobserved or inapplicable, never zero or success. The manifest binds immutable sanitized environment and artifact identity once; events refer to it through capture_id.

1. P003-TASK-010 performs the read-only reconciliation before enabling capture. It records task 310 and task 317 source locators, candidate commits, artifact hashes, generator and test fingerprints, row identities, dispositions, and count deltas. It preserves compatible input and reports missing or contradictory joins as open without launching a product process or collecting capture.
2. P003-TASK-010 records candidate commit, JAR hashes, NeoForge 21.1.235, LuckPerms 5.4.140, configuration fingerprint, fixture identity, node-1 runtime path, exact cleanup paths, and `eula=true` readback for any disposable server. It enables the smallest category and grammar-valid target, for example `sef debug on command action:<verified-catalog-id> 60 1000`, runs `sef debug status`, and confirms schema_version, enabled, capture_id, side, category, sanitized_target, remaining_ms, accepted, dropped, truncated, remaining_events, remaining_bytes, output `logs/sef/diagnostics/<capture_id>/events.jsonl`, and stop_reason.
3. P003-TASK-010 tests an unauthorized player enable, then authorized console enable without changing permissions. It tests the observed doctor route by discovering its exact permission from the live dispatcher, granting only that node to playera, checking tree refresh and execution, applying an exact deny, and confirming the deny wins. Operator status alone remains neither a grant nor a product-failure oracle.
4. P003-TASK-010 executes provider-only then matching-SEF NeoForge 21.1.235 and LuckPerms 5.4.140 fixtures with the same synthetic identity. Within 60 seconds of ready state it records capability initialization, identity validity, exception origin, lifecycle order, direct grant, direct deny, revocation, refresh, reconnect, and outage fallback. It classifies timeout as startup, capability, identity, projection, or policy, repairs only a proven fixture or SEF defect, and never weakens login or authorization.
5. P003-TASK-010 executes the real command fixtures from the failure table and inspects `jq -c 'select(.capture_id == "<returned-id>" and .correlation_id == "<operation-id>")' logs/sef/diagnostics/<capture_id>/events.jsonl`. It joins the returned events and `manifest.json` to before and after state, feedback, terminal audit, writer, receipt, restart, and recovery records. Missing an event, field, audit, or matching state leaves that dimension open.
6. P003-TASK-010 runs `sef debug off`, then status, repeats off, and issues a matching stimulus to prove capture stopped. It separately tests malformed limits, busy capture, absent or removed target, 300-second maximum, 10000-event maximum, 4 MiB output, 100 accepted events per second, 20 accepted events per tick, 1 ms per tick, timeout, reload, shutdown, restart, sink failure, and diagnostics-off versus diagnostics-on outcome equality.
7. P003-TASK-010 retains one immutable sanitized support snapshot containing manifest, bounded events, project, loader, JVM, artifact identity, minimal nonsecret configuration, reproduction, expected and actual state, capture and correlation identifiers, and test summary. It reviews redaction, stops exact owned processes, verifies exit, then removes only exact owned runtimes, worlds, logs, captures, reports, and scratch paths after their final consumer.

## Verification Matrix

| Requirement or task | Static or unit | Integration | Real workflow or runtime | Negative and recovery | Execution host and prerequisites | Evidence artifact |
|---|---|---|---|---|---|---|
| P003-TASK-010, SEFAUD-REQ-004 | Schema, capture, redaction, provider bridge, native ABI and JAR tests. | Reconcile task 310 and task 317, provider-only and matching-SEF fixtures. | node-1 disposable server console; laptop only for residual tree and feedback claims. | Unauthorized capture, limit, reset, native error, denied doctor, failed login. | node-1 no GUI; laptop verified only if client claim remains; EXT-001, EXT-002. | Capture status, matrix delta, provider diagnosis, native manifest. |
| P003-TASK-011, SEFAUD-REQ-004 | Schema and permission projection tests. | Generic and indirect unavailable transitions. | Dedicated-server console, synthetic roles, GUI fallback only when needed. | Sixteen families unchanged after all access contexts. | node-1 no GUI. | Negative row ledger and sanitized audit observations. |
| P003-TASK-012, SEFAUD-REQ-004 | Dispatcher, policy, domain, route and matrix tests. | Multi-actor, restart, receipt and audit joins. | Configured no-GUI dedicated server and verified server-only GameTest task. | Missing dimension, failed oracle, unknown outcome, stale evidence. | node-1, exact candidate and disposable runtime. | Full action matrix and command reports. |
| P003-TASK-013, SEFAUD-REQ-004 | Focused regression after each repair. | Affected dependency and store joins. | Original failing real entry point. | Reproduce, repair, recover, rerun invalidated rows. | Same verified host and fixture identity. | Finding ledger and rerun map. |
| P003-TASK-014, SEFAUD-REQ-004 | Generator and documentation drift tests. | Concurrency, queue, schedule, and audit load fixtures. | Bounded dedicated-server workload. | Threshold breach, retained worker, queue overflow, documentation drift. | node-1 no GUI. | Baseline report, generated references, sanitized evidence index. |
| P003-TASK-015, SEFAUD-REQ-004 | Clean candidate commands discovered from configured Gradle tasks. | All affected phase checks and review gates. | Required dedicated-server and scoped laptop residual reruns. | Stale candidate, failed check, pending merge, tag, or cleanup failure. | Matching candidate, exact host contract, no foreign runtime requirement. | SEFAUD-IF-006 completion packet. |

## Documentation, Operations, and Release

P003-TASK-010 documents SEFAUD-IF-004 in DOCUMENTATION.md with exact authorization, status, bounds, safe failure, redaction, output discovery, disabling, retention, and cleanup. P003-TASK-014 regenerates references and updates only behavior proven by this phase. README.md and docs/README.md change when public command or documentation navigation changes. P003-TASK-015 prepares wiki updates from merged tracked documentation only and never publishes a release.

## Risks and Evidence Invalidation

| Risk ID and owner task | Prevention | Detection | Recovery | Evidence invalidated | Reverification |
|---|---|---|---|---|---|
| RISK-001, P003-TASK-010 | Preserve input identity and distinct dispositions. | Task 310 task 317 counts and joins disagree. | Repair reconciliation, retain compatible proof. | Matrix aggregate and dependent selection. | Regenerate current rows and explain delta. |
| RISK-002, P003-TASK-010 | Current authority and redacted mandatory audit. | Wrong actor, state change after deny, sensitive sink marker. | Fail closed, repair owner, rerun routes. | Authority, feedback, audit, route evidence. | Multi-actor real entry fixture. |
| RISK-004, P003-TASK-010 | Same opened-object checks and ABI review. | Link, swap, metadata, write, flush, or linkage failure. | Preserve state and repair native boundary. | Sink joins and dependent action rows. | Linux source, artifact, and runtime fixture. |
| RISK-005, P003-TASK-008 | Receipts, ordered publication, owned synthetic paths. | Unknown outcome or diverged state hashes. | Resolve receipt before retry. | Action store and restart proof. | Failure cut and restart fixture. |
| RISK-007, P003-TASK-010 | Provider-only comparison and fail-closed policy. | Login stage and tree or server result differ. | Repair fixture or confirmed defect, reauthenticate. | Authorization and client-projection proof. | Grant, deny, revoke, refresh, reconnect, outage. |

## Phase Completion Packet

P003-TASK-015 records candidate source and artifact hashes, task 310 and task 317 reconciliation, SEFAUD-IF-004 controls and self-tests, exact provider diagnosis, action matrix, unavailable-family ledger, native and dependency evidence, test commands and reports, remediation reruns, references and documentation diffs, performance results, cleanup records, review, PR, merge commit, resulting candidate check, signed tag, and next transition. Each record names owned resources and confirms process exit plus exact scratch cleanup. Required sanitized evidence is preserved once outside disposable output.

## Next Transition

After P003-TASK-015 has completed all phase gates and the phase merge is present on the approved candidate branch, advance only to SEFAUD-PHASE-004. Its entry action consumes the integrated command matrix and SEFAUD-IF-004 capture contract for UI, accessibility, privacy, and fallback verification.
