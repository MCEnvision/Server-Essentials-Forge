# Phase 000 Execution Plan

> **Plan ID:** PLAN-PHASE-000  
> **Phase ID:** SEFAUD-PHASE-000  
> **Owner:** Repository audit contract  
> **Classification:** MANDATORY  
> **Master plan:** [plan.md](../plan.md)  
> **Phase sequence:** 000 of 007

## Purpose and Ownership

This historical, signed, and merged phase established the inventory and completion-packet foundations. Its stable task identities, delivered doctor and report controls, evidence packet, merge `a71840c96ea507b438213be4967ae5af4398c0c1`, and signed `phase-000-audit` tag remain historical facts. The current master retrospectively maps the original baseline outputs to logical `SEFAUD-IF-001` and `SEFAUD-IF-006`; it does not assert that the historical packet used every later interface name or capture schema. The master owns product scope and phase topology. This blueprint owns only the original detailed meaning of `SEFAUD-PHASE-000` and `P000-TASK-001` through `P000-TASK-014`.

The phase did not depend on the later `SEFAUD-IF-004` capture system. It used the original doctor, health, generator, and report controls. A current aggregate repair, a new capture requirement, or a changed matrix input belongs to Phase 003 task 010; it does not restart, relabel, or invalidate the Phase 000 integration. Reuse of historical evidence is conditional on candidate, artifact, loader, dependency, configuration, fixture, harness, assertion, and cleanup compatibility.

## Evidence-Based Entry State

| Evidence class | Area | Finding | Source or command | Freshness condition |
|---|---|---|---|---|
| VERIFIED | Historical integration | PR 10 merged the delivered baseline into `master` and the Phase 000 annotated tag signature was verified. | master Section 4, research brief | Reuse is invalid for an intersecting source, artifact, fixture, harness, or assertion change. |
| OBSERVED | Current candidate | `envy/phase-003-commands` at `e160a235b19c992b3a23c3a43754e92ad0147948` is active, with no Phase 003 PR observed. | research brief, intake FIND-002 | This records continuity, not Phase 000 work to rerun. |
| OBSERVED | Aggregate disagreement | Historical task 310 and task 317 have incompatible command-matrix classifications. | research brief, intake FIND-003 | Current P003 reconciliation must regenerate all compatible joins. |
| VERIFIED | Original diagnostics | Doctor, health, inventory generator, reference generator, dependency manifest, and report controls were delivered; new debug capture was not. | research brief, repository map | The original controls remain valid only at their recorded inputs. |
| OBSERVED | Portable runtime contract | Minecraft Java is portable across Linux, macOS, and Windows. The Linux laptop is the single execution fixture for graphical client evidence, node-1 is headless only, and no foreign client or native-runtime gate exists. | DEC-009, EXT-001 | Any newly exercised host-specific non-client path needs its own matching evidence. |
| OBSERVED | Dependency closure | EXT-001 and EXT-002 are available owned work, not external blockers. | master Section 10, intake FIND-006, FIND-007 | Current advisory and artifact changes require their owning current phase review. |

## Scope Boundaries

### Included Scope

- `SEFAUD-REQ-001` historical delivery of complete surface ownership, deterministic evidence reconciliation, original diagnostic/report controls, `SEFAUD-IF-001`, and `SEFAUD-IF-006`.
- Inventory participation for `SEFAUD-IF-002`, `SEFAUD-IF-003`, `SEFAUD-IF-004`, and `SEFAUD-IF-005`: Phase 000 records their source, owner, consumer, evidence route, and invalidation boundary without implementing their later behavior.

### Explicit Exclusions

- `SEFAUD-REQ-002`, `SEFAUD-REQ-003`, `SEFAUD-REQ-004`, `SEFAUD-REQ-005`, `SEFAUD-REQ-006`, `SEFAUD-REQ-007`, `SEFAUD-REQ-008`, and `SEFAUD-REQ-009` have later canonical owners.
- `SEFAUD-IF-004` implementation belongs to Phase 003 task 010; Phase 000 records it as proposed and never claims it was delivered here.
- `FUT-001`, `FUT-002`, `FUT-003`, `NG-001`, `NG-002`, `NG-003`, `NG-004`, `NG-005`, and `NG-006` remain excluded. The sixteen unavailable families remain negative inventory rows, not functionality to add.

## Phase Contract

### SEFAUD-PHASE-000 — Authoritative Audit Baseline and Traceability

**Objective:** Preserve one deterministic historical baseline with unique audit owners, evidence identities, original operational controls, and a rejecting completion-packet schema.  
**Owner:** Repository audit contract  
**Dependencies:** DEC-002, DEC-003, DEC-004, DEC-005, DEC-006, DEC-007, DEC-008, DEC-009  
**Canonical requirements:** SEFAUD-REQ-001  
**Documentation and release impact:** Historical generated references and baseline documentation were reconciled at the merged candidate. Current documentation changes are made only by the phase that proves current behavior. No publication occurred.  
**Next transition:** SEFAUD-PHASE-001

**Entry criteria**

- The approved SEF 2 candidate lineage, exact integration target, source tree, toolchain, and unrelated working state were identified without importing legacy `forge-1.20.1` work.
- The phase branch was based on the approved candidate and registered disposable evidence paths before any test, audit, or runtime work.
- The actual configured task graph was inspected before claiming a task headless; every server runtime read back `eula=true` before launch.

**Implementation scope**

- `SEFAUD-REQ-001` delivered the source-to-evidence inventory, deterministic joins, invalidation graph, original doctors and reports, and completion schema needed by later phases.
- `SEFAUD-REQ-001` retrospectively maps the historical evidence and packet outputs to `SEFAUD-IF-001` and `SEFAUD-IF-006`; inventory rows declare the later producers and consumers of `SEFAUD-IF-002`, `SEFAUD-IF-003`, `SEFAUD-IF-004`, and `SEFAUD-IF-005` without competing implementations.

**Execution order**

1. `P000-TASK-001` executed `SEFAUD-REQ-001` candidate and host provenance preservation.
2. `P000-TASK-002` executed `SEFAUD-REQ-001` evidence identity, sanitation, retention, cleanup, and invalidation definition.
3. `P000-TASK-003` executed `SEFAUD-REQ-001` trust-boundary inventory for Phase 001.
4. `P000-TASK-004` executed `SEFAUD-REQ-001` command and unavailable-family inventory for Phase 003.
5. `P000-TASK-005` executed `SEFAUD-REQ-001` UI and fallback-surface inventory for Phase 004.
6. `P000-TASK-006` executed `SEFAUD-REQ-001` durable and transient-state inventory for Phase 002 and later command joins.
7. `P000-TASK-007` executed `SEFAUD-REQ-001` lifecycle and integration inventory for Phase 005.
8. `P000-TASK-008` executed `SEFAUD-REQ-001` supply, native, and advisory evidence inventory for Phase 001.
9. `P000-TASK-009` executed `SEFAUD-REQ-001` joins for tests, hosts, documents, and interface consumers.
10. `P000-TASK-010` executed `SEFAUD-REQ-001` validation and documentation of existing doctors and report controls.
11. `P000-TASK-011` executed `SEFAUD-REQ-001` baseline generation twice using the original controls.
12. `P000-TASK-012` executed `SEFAUD-REQ-001` correction of Phase 000 machinery and routing of product findings.
13. `P000-TASK-013` executed `SEFAUD-REQ-001` assembly of the sanitized packet and independent review disposition.
14. `P000-TASK-014` executed `SEFAUD-REQ-001` approved pull request integration, merge verification, and phase-tag signing.

**Required evidence**

- Two normalized inventory bundles, source and live joins, source/artifact/fixture fingerprints, original doctor/report outputs, generated-reference drift result, host and prerequisite ledgers, and exact cleanup receipts.
- A `SEFAUD-IF-001` matrix with one owner and proof route per mandatory row, and a `SEFAUD-IF-006` packet rejecting missing checks, stale artifacts, contradictory evidence, or incomplete cleanup.

**Exit criteria**

- `SEFAUD-AC-001`, `SEFAUD-AC-002`, and `SEFAUD-AC-003` were satisfied at the merged historical candidate, with no missing, duplicate, stale-current, orphaned, or unowned Phase 000 row.
- The current master can map the baseline to all six shared interfaces: historical Phase 000 outputs support IF-001 and IF-006, while the recorded inventories identify later owners and evidence routes for IF-002, IF-003, IF-004, and IF-005 without claiming their later schemas were historical packet fields.
- The original controls remained distinct from planned capture; no historical packet claims `/sef debug on`, `status`, or `off` existed.
- The required checks, review disposition, merge, resulting candidate verification, signed tag, and cleanup were complete.
- No known mandatory phase-owned defect remains.

## Inputs and Upstream Contracts

| Input or contract | Provider | Required state | Validation | Failure behavior |
|---|---|---|---|---|
| Candidate identity | DEC-006, repository metadata | Commit, tree, lineage, artifact inputs, and integration target are explicit. | Compare ancestry, tree, and source fingerprints. | Stop integration on legacy target or mixed identity. |
| Portable runtime | DEC-009, EXT-001 | One portable Minecraft Java contract uses the Linux laptop only as the graphical execution fixture, with node-1 reserved for headless server work. | Host roles and actual exercised paths are recorded. | Preserve only affected row unverified, continue independent headless work. |
| Dependency evidence | EXT-002 | Declared, resolved, packaged, installed, API, reachability, provenance, and remedy dimensions remain separate. | Foreign-key and JAR duplicate checks. | Route applicable exposure to Phase 001. |
| Existing doctors and reports | Repository controls | Syntax, authorization, expected scope, output, and retention route are discovered. | Original focused tests and report validation. | Record failed or unknown capability, never substitute planned capture. |

## Outputs and Downstream Contracts

| Output or contract | Consumer | Guaranteed state | Compatibility or versioning | Evidence |
|---|---|---|---|---|
| SEFAUD-IF-001 logical mapping | SEFAUD-PHASE-001, SEFAUD-PHASE-002, SEFAUD-PHASE-003, SEFAUD-PHASE-004, SEFAUD-PHASE-005, SEFAUD-PHASE-006, SEFAUD-PHASE-007 | The current master maps historical candidate, surface, proof, disposition, and invalidation identity to the logical contract. | Duplicate, stale, missing, and incompatible current schema rows reject. | Historical normalized bundles and current compatibility validation. |
| SEFAUD-IF-006 logical mapping | SEFAUD-PHASE-001, SEFAUD-PHASE-002, SEFAUD-PHASE-003, SEFAUD-PHASE-004, SEFAUD-PHASE-005, SEFAUD-PHASE-006, SEFAUD-PHASE-007 | The current master maps historical requirements, tasks, evidence, checks, review, merge, tag, cleanup, and next phase to closure fields. | No current closure on a queued PR, stale artifact, or failed cleanup. | Historical packet, signed tag, and current compatibility validation. |
| IF-002, IF-003, IF-004, IF-005 inventory mappings | Their master-declared producer and consumer phases | Current mapping identifies historical source rows and later ownership without claiming later implementation or schema fields. | Later behavior changes invalidate only intersecting proof. | Requirement-to-interface registry. |

## Work Packages

| Task ID | Requirement IDs | Work | Inputs and dependencies | Outputs | Affected components or interfaces | Verification |
|---|---|---|---|---|---|---|
| P000-TASK-001 | SEFAUD-REQ-001, DEC-006, DEC-009 | Freeze the historical candidate, branch, host, toolchain, dirty-state, and preservation record. | DEC-006, DEC-009 | Candidate and host manifest. | Git metadata, Gradle inputs, mod metadata, EXT-001. | Repeated read-only collection agrees and excludes legacy integration. |
| P000-TASK-002 | SEFAUD-REQ-001, DEC-005 | Define row identity, sanitizer, retention, invalidation, cleanup registry, and packet foreign keys. | P000-TASK-001, DEC-005 | Versioned evidence schema. | Audit evidence contract, report writers, SEFAUD-IF-001, SEFAUD-IF-006. | Reject missing fields, duplicate rows, unsafe roots, private data, stale identity, and absent cleanup. |
| P000-TASK-003 | SEFAUD-REQ-001, DEC-001, DEC-003 | Enumerate privileged inputs, indirect executors, protected sources, validators, sinks, redaction, and findings. | P000-TASK-001, P000-TASK-002 | Trust-boundary inventory for Phase 001. | Command kernel, GUI protocol, config, audit, export, native writer, SEFAUD-IF-002, SEFAUD-IF-005. | Synthetic omitted validator, hidden sink, reflection target, and orphan boundary reject. |
| P000-TASK-004 | SEFAUD-REQ-001, DEC-002, DEC-008 | Join live dispatcher, catalog, shortcuts, permissions, indirect routes, and unavailable families by canonical action. | P000-TASK-001, P000-TASK-002 | Command dimension inventory for Phase 003. | KernelServices, command catalog, registrars, permission registry, SEFAUD-IF-002. | Exact identity joins reject alternate bypass, orphan route, collision, and advertised unavailable family. |
| P000-TASK-005 | SEFAUD-REQ-001, DEC-008, DEC-009 | Inventory every screen, menu, HUD, workflow, payload, translation, narration, textual feedback, and fallback. | P000-TASK-001, P000-TASK-002, P000-TASK-004 | UI and feedback inventory for Phase 004. | GUI catalog, SefGuiServer, session manager, language resources, SEFAUD-IF-002. | Source, descriptors, payload codecs, action ids, and fallback rows reconcile; private or orphan projection rejects. |
| P000-TASK-006 | SEFAUD-REQ-001, DEC-005, DEC-009 | Inventory all repository and nonrepository durable writers, transient owners, schema, recovery, and native opened-object rules. | P000-TASK-001, P000-TASK-002 | Store and transient-state inventory for Phases 002, 003, 005. | StorageCoordinator, AtomicFileStore, workers, provider writers, SEFAUD-IF-003, SEFAUD-IF-005. | Implementation, registration, writer, serializer, path, worker, and test sets reconcile. |
| P000-TASK-007 | SEFAUD-REQ-001, DEC-004, DEC-009 | Map construction, reload, connection, provider, side, thread, queue, stop, and recovery ownership. | P000-TASK-001, P000-TASK-002 | Lifecycle and adapter inventory for Phase 005. | ServerEssentialsForge, KernelServices, event handlers, adapters, payload handlers. | Missing cleanup, client class on server, unsafe fallback, stale authority, and unbounded worker reject. |
| P000-TASK-008 | SEFAUD-REQ-001, DEC-004, DEC-009, EXT-002 | Capture declared, resolved, packaged, installed, API, ABI, native-load, license, provenance, and advisory dimensions. | P000-TASK-001, P000-TASK-002, EXT-002 | Supply and native evidence ledger for Phase 001. | build.gradle, wrapper, JAR, NeoForge runtime, NativeAuditFileProvider, SEFAUD-IF-005. | Graph and JAR checks reject missing provenance and duplicate JNA classes, services, or native runtime. |
| P000-TASK-009 | SEFAUD-REQ-001, DEC-009, EXT-001, EXT-002 | Join tests, task graphs, hosts, docs, claims, prerequisites, and later-phase proof routes. | P000-TASK-003, P000-TASK-004, P000-TASK-005, P000-TASK-006, P000-TASK-007, P000-TASK-008 | Traceability graph and finding register. | Tests, GameTests, docs, host matrix, all SEFAUD-IF-001 through SEFAUD-IF-006. | Every mandatory row has valid foreign keys; no client is assigned to node-1; no foreign-client gate exists. |
| P000-TASK-010 | SEFAUD-REQ-001 | Validate original doctor, health, inventory, dependency, reference, and report controls; document their limits. | P000-TASK-002, P000-TASK-003, P000-TASK-009 | Original diagnostics register and support procedure. | Existing doctor commands, subsystem reports, generators, SEFAUD-IF-001. | Authorization, malformed input, absent target, report schema, redaction, and cleanup checks pass at historical fidelity. |
| P000-TASK-011 | SEFAUD-REQ-001, DEC-005, DEC-009, EXT-001, EXT-002 | Run historical baseline generation twice, use original controls, and bind live/static sets to the candidate. | P000-TASK-004, P000-TASK-005, P000-TASK-006, P000-TASK-007, P000-TASK-008, P000-TASK-009, P000-TASK-010 | Two normalized bundles, host ledger, cleanup reports. | generateAuditInventory, dependency manifest, project references, JAR inspection, SEFAUD-IF-001. | Normalized digests and semantic rows agree; every owned runtime and scratch path is cleaned. |
| P000-TASK-012 | SEFAUD-REQ-001 | Repair only baseline tooling, schema, evidence, or documentation defects and route product defects. | P000-TASK-011 | Frozen historical baseline and later findings. | Generators, validators, generated references, evidence schema. | Rerun all invalidated rows; reject external-blocker wording, stale-current claim, and unowned finding. |
| P000-TASK-013 | SEFAUD-REQ-001, DEC-009 | Audit diff, evidence privacy, support claims, artifacts, cleanup, and independent review before closure. | P000-TASK-012 | Sanitized completion packet and review disposition. | Evidence root, documentation, reports, SEFAUD-IF-006. | Secret, path, privacy, stale claim, cache, runtime, or incomplete packet prevents closure. |
| P000-TASK-014 | SEFAUD-REQ-001, DEC-006, DEC-009 | Commit, PR, checks, review, merge, resulting-branch verification, tag, and Phase 001 handoff. | P000-TASK-013, DEC-006 | Merged baseline, signed tag, handoff. | GitHub integration, SEFAUD-IF-006. | Correct target, required checks, signature, clean post-merge generation, and cleanup are mandatory. |

Tasks 003 through 008 may collect independent source inventories after task 002. Task 009 waits for them. Tasks 010 through 014 are strictly ordered. A failed inventory test restores only the owned disposable fixture, retains a sanitized failure record, marks affected rows failed or stale, and blocks the dependent join; it never fabricates a pass or changes later product behavior.

## Architecture and Implementation Boundaries

The baseline follows the existing component direction: command and GUI requests enter policy before domain mutation, durable publication remains owned by storage, native audit appends bind validation and flush to one verified object, optional integrations report explicit state, and client caches remain presentation. `SEFAUD-IF-001` records exact candidate and proof identity rather than inferring behavior from counts. `SEFAUD-IF-006` records closure only after all gates. IF-002 action outcome, IF-003 durable outcome, IF-004 capture status, and IF-005 append outcome are owned by their master-assigned phases; Phase 000 inventories their schemas and callers without duplicate implementations.

EXT-001 records the portable Minecraft Java contract, source/artifact native analysis, available Linux execution, and honest limits. The laptop is an execution fixture, not the product contract. EXT-002 separates coordinate ownership, packaging, runtime supply, API/configuration reachability, provenance, advisory conditions, and remedy. No operating-system-specific client test or foreign native execution is introduced. The 16 unavailable families have stable negative rows and no runtime capability claim.

## Failure, Recovery, and Edge Cases

| Scenario | Detection | Required behavior | Recovery or rollback | Regression proof |
|---|---|---|---|---|
| Missing or duplicate surface | IF-001 exact-set validator reports row id, category, owner, and source locator. | Reject reconciliation before downstream use. | Add or de-duplicate the owned row, regenerate affected category and joins. | Omitted and duplicate row mutation fixtures. |
| Stale or mixed candidate evidence | Candidate commit, artifact hash, fixture, or harness fingerprint differs. | Mark intersecting row stale, never pass by historical count. | Preserve compatible historical evidence and rerun only intersecting scope in current owner phase. | Candidate-mismatch validator and invalidation graph check. |
| Original doctor unavailable or denied | Dispatcher discovery, authorization result, output schema, and safe reason. | Record capability as denied, failed, or unknown; do not use IF-004 as a substitute. | Correct historical control only if a Phase 000 defect; otherwise route finding. | Authorized, denied, malformed, and absent-target report checks. |
| Unsafe evidence or cleanup escape | Sanitizer, root identity, symlink check, and teardown receipt fail. | Stop capture or report generation before retention. | Remove only verified owned scratch, preserve sanitized failure summary, re-run. | Unsafe-path, secret marker, excessive-size, interrupted-teardown fixtures. |
| Host or native claim exceeds evidence | Host ledger finds a new exercised non-client branch or source/artifact analysis differs. | Keep the exact row unverified and distinguish analysis from execution. | Obtain matching available evidence or route the changed row to its owner. | Source-to-runtime reachability and artifact comparison. |
| Matrix aggregate conflict | Per-row provenance identifies a missing compatible input or changed classification semantics. | Do not rewrite historical Phase 000 completion. | Phase 003 task 010 repairs the current aggregate and regenerates it. | Task 310/317 input-level reconciliation. |

## Diagnostics and Debugging

**Requirement IDs:** SEFAUD-REQ-001  
**Task IDs:** P000-TASK-002, P000-TASK-009, P000-TASK-010, P000-TASK-011, P000-TASK-013  
**Controls:** Existing `/sef doctor` and subsystem doctor/report controls are discovered from the actual dispatcher, then used only with their discovered authorization and syntax. Inventory, dependency-manifest, reference, and drift reports are the Phase 000 report controls. `SEFAUD-IF-004` `/sef debug on`, `status`, and `off` is a later P003 control, not a historical Phase 000 command.  
**Signals:** Candidate commit, artifact SHA-256 and SHA-512, row id, category, owner, source locator, proof id, expected and actual classification, result, invalidation cause, host role, report schema version, cleanup state, and safe reason code distinguish baseline failure from product behavior.  
**Collection procedure:** Follow the numbered historical procedure below: bind a report to its candidate and inputs, invoke the discovered existing control or generator, validate the report and reconciliation, redact and retain only the packet, then verify cleanup.  
**Headless verification:** Inspect the configured Gradle task graph before use. Run only original report and generator checks that prove no client, renderer, or display starts; use a dedicated no-GUI server only when the original control needs server state. The result proves baseline/report fidelity, not UI rendering or a later capture system.  
**Client verification:** No Minecraft client is required for this historical inventory and report contract. The old client-surface registration observation is a bounded inventory input, not visual acceptance. Phase 004 owns current client proof; no server report substitutes for it.  
**Client audio isolation:** This phase starts no Minecraft client. If historical evidence is revalidated with a client as a specifically named residual claim, the later execution phase must set the isolated instance master audio to zero before launch, bind the owned Hyprland window address, class, title, and PID with `hyprctl clients -j`, mute and read back only its correlated PipeWire or PulseAudio stream with `wpctl` or `pactl`, remute replacements, then remove the watcher and verify stream disappearance.  
**Budgets and privacy:** Reports are bounded to the declared candidate and category, retain relative evidence ids and digests, redact credentials, tokens, private messages, addresses, hidden identities, restricted arguments, private absolute paths, and raw worlds. Existing controls are used at their verified limits; unknown limits are recorded as unknown, not invented. Failed or oversized reports stop and retain only a sanitized failure summary.  
**Regression and support:** Validate report schemas, authorization, malformed input, missing target, stale identity, omission, duplicate, redaction, cleanup, and exact-set rejection. Document the original support procedure and evidence location in the historical packet. P003 later documents capture enable, status, disable, expiry, counters, and collection without attributing them to Phase 000.

| Signal | Source and unit | Expected observation |
|---|---|---|
| candidate identity | repository metadata, commit and SHA-256/SHA-512 | Every report and proof resolves to one candidate and artifact. |
| inventory identity | generator report, row id and semantic key | One owner, requirement, phase route, and source locator per mandatory row. |
| report result | existing doctor or report, enum and safe reason | `passed`, `failed`, `open`, `partial`, and `not_applicable` remain distinct. |
| invalidation | evidence registry, changed fingerprint and row id | Exact intersecting rows become stale without erasing compatible history. |
| cleanup state | teardown registry, path and process count | Every owned resource is removed or a precise leftover blocks packet closure. |

1. Register a unique disposable report root, exact candidate, artifact identity, inputs, source-control syntax, expected result, host role, and all owned paths or processes. If server state is required, create or correct that runtime's `eula.txt`, read back effective `eula=true`, and verify the configured task starts no client.
2. Discover the existing doctor or report control from the dispatcher and source evidence. Run the smallest authorized report or generator with a synthetic fixture; record denied, malformed, absent-target, and normal outcomes without using a player or console to bypass the checked policy.
3. Validate schema version, row foreign keys, candidate fingerprints, expected and actual result, sanitizer output, and cleanup registration. Reconcile exact semantic identities rather than counts. A mismatch produces an actionable row and rerun scope.
4. Stop only owned processes, preserve the minimal sanitized report and digest, delete verified disposable paths after their final consumer, confirm paths and processes are gone, and record cleanup separately. No runtime control is left enabled because no Phase 000 capture control exists.

## Verification Matrix

| Requirement or task | Static or unit | Integration | Real workflow or runtime | Negative and recovery | Execution host and prerequisites | Evidence artifact |
|---|---|---|---|---|---|---|
| P000-TASK-001, P000-TASK-002 | Metadata and schema validators. | Candidate and packet foreign-key join. | Read-only provenance collection. | Mixed commit, duplicate id, unsafe root reject. | node-1, no runtime required. | Candidate manifest, schema report, cleanup receipt. |
| P000-TASK-003, P000-TASK-004, P000-TASK-005 | Source and generator exact-set tests. | Catalog, permission, GUI descriptor, and reference joins. | Original dispatcher inventory when server task is proven no-GUI. | Omitted route, private projection, alternate sink, unavailable claim reject. | node-1 dedicated server only if required, `eula=true` readback. | Surface bundles and finding registry. |
| P000-TASK-006, P000-TASK-007, P000-TASK-008 | Writer, lifecycle, graph, JAR, and schema checks. | Registration, adapter, native provider, and dependency joins. | Available Linux native analysis and historical compatible proof only. | Unmanaged writer, client classload, duplicate JNA, stale advisory dimension reject. | node-1 headless source/artifact analysis, no foreign runtime gate. | Store, lifecycle, supply, EXT ledgers. |
| P000-TASK-009, P000-TASK-010 | Task-graph and report-schema checks. | Test, host, document, doctor, and interface registry join. | Existing no-GUI doctor/report path only if discovered. | Denial, malformed request, missing target, stale report, cleanup failure. | node-1 headless or source-only; no client. | Diagnostics register and traceability report. |
| P000-TASK-011, P000-TASK-012 | Deterministic generator and drift tests. | Two full normalized bundle reconciliations. | Original server report fixture only where needed. | Input removal, duplicate, stale artifact, interrupted teardown. | node-1, inspected task graph, disposable runtime only if necessary. | Bundle digests, discrepancy register, cleanup receipt. |
| P000-TASK-013, P000-TASK-014 | Packet, signature, privacy, and diff validators. | Review, PR checks, merge, resulting candidate, tag. | Clean post-merge report regeneration. | Queued PR, unsigned tag, failed check, incomplete cleanup reject closure. | Approved repository workflow, no product client. | SEFAUD-IF-006 packet, merge and tag evidence. |

## Documentation, Operations, and Release

Historical outputs included generated command, permission, and configuration references; baseline facts in `README.md`, `DOCUMENTATION.md`, `test.md`, `audit.md`, `docs/SEF2_ACCEPTANCE.md`, `docs/COMPATIBILITY_MATRIX.md`, `docs/SECURITY_REVIEW.md`, `docs/PERFORMANCE_REPORT.md`, and `docs/RELEASE_WORKFLOW.md`; and a report-support procedure. Claims remain revision-scoped. Current phases update those documents only when their own evidence proves current behavior. Documentation states the portable Minecraft Java contract, identifies the Linux laptop only as its graphical execution fixture, distinguishes analysis from runtime proof, describes EXT work as available, and does not claim capture controls, unavailable families, foreign-host requirements, publication, production work, or zero hosted alerts.

## Risks and Evidence Invalidation

| Risk ID and owner task | Prevention | Detection | Recovery | Evidence invalidated | Reverification |
|---|---|---|---|---|---|
| RISK-001, P000-TASK-002, P000-TASK-009 | Stable semantic keys and exact input fingerprints. | Missing, duplicate, orphan, or count-only row. | Repair baseline machinery or route current aggregate to Phase 003 task 010. | Intersecting matrix rows and dependent joins. | Regenerate changed category and all affected joins. |
| RISK-002, P000-TASK-003, P000-TASK-004 | Complete source-to-sink and route inventory. | Direct executor, redaction sink, or route lacks owner. | Route defect to Phase 001 or Phase 003 without changing history. | Trust or command rows affected by source change. | Re-run current owner boundary regression. |
| RISK-003, P000-TASK-006, P000-TASK-008 | Separate writer identity and supply dimensions. | Path-only write, ABI mismatch, duplicate runtime, missing provenance. | Fail closed, preserve evidence, route remediation. | Native, storage, and dependency rows. | Current source/artifact analysis and available Linux fixture. |
| RISK-008, P000-TASK-013, P000-TASK-014 | Packet rejects wrong branch, stale artifact, unfinished checks, and cleanup gaps. | Merge target, signature, tag, or receipt mismatch. | Stop closure and correct approved workflow. | Completion packet and downstream phase entry. | Recheck resulting candidate and signed tag. |
| RISK-009, P000-TASK-001, P000-TASK-011 | Exact disposable-resource registry and host boundary. | Forbidden client task, unowned stream, process, or leftover path. | Stop owned process only, clean exact owned resources, leave gate unverified. | Affected runtime report and cleanup receipt. | Host preflight and teardown verification. |

## Phase Completion Packet

The immutable sanitized `phase-000/final/` packet contains the historical source and merge commits, candidate lineage, signed PR and tag verification, exact source and artifact manifests, two normalized inventory bundles, interface registry for SEFAUD-IF-001 through SEFAUD-IF-006, original doctor/report schema and results, host and EXT ledgers, dependency/native dimensions, generated-reference drift, later finding register, privacy and diff review, cleanup accounting, required check and review disposition, and Phase 001 handoff. It keeps relative evidence ids and digests only, never private absolute paths, raw logs, worlds, credentials, personal data, whole runtimes, or unrelated user state.

Every historical test, build, report, and audit registers exact scratch paths, processes, server runtime, logs, reports, traces, downloads, bytecode, and evidence before it starts. It stops owned processes, verifies exit, preserves only required sanitized evidence, removes only verified test-created resources after their final consumer, and records precise leftovers as a closure failure. Existing source, personal instances, saves, worktrees, caches, branches, tags, and user data remain protected.

## Next Transition

The historical transition to `SEFAUD-PHASE-001` occurred only after PR 10 merged into the approved candidate branch, the resulting candidate regenerated the baseline and references, the packet validated, cleanup completed, and `phase-000-audit` was signed. Phase 001 consumed the frozen trust, data-flow, writer, native, supply, EXT, and inventory evidence. The current master maps that evidence to the shared-interface terminology. Current execution remains in Phase 003. A missing, stale, duplicate, or incompatible current aggregate row triggers Phase 003 task 010 reconciliation and affected current-phase proof; it does not reopen Phase 000, alter its merge, or create a backward dependency on SEFAUD-IF-004.
