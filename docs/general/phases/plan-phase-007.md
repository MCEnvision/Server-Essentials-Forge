# Phase 007 Execution Plan

> **Plan ID:** PLAN-PHASE-007
> **Phase ID:** SEFAUD-PHASE-007
> **Owner:** Documentation evidence
> **Classification:** MANDATORY
> **Master plan:** [plan.md](../plan.md)
> **Phase sequence:** 007 of 007

## Purpose and Ownership

This final phase reconciles every current documentation claim and every sanitized evidence reference with one final integrated SEF 2 candidate, proves plan-wide traceability and the complete Definition of Done, and records release readiness without publishing or deploying anything. It owns `SEFAUD-REQ-009` and the final closure decision. It does not repeat Phase 006 by accepting historical results at face value. It validates the Phase 006 completion packet, freezes the exact product inputs and artifact, replays documentation-facing operator procedures, rejects stale or lower-fidelity evidence, and runs the final cross-matrix audit. Every user-facing and operator-facing support document must identify Linux, macOS, and Windows as supported Minecraft Java environments. The final documentation must also explain the provider-specific or native opened-object identity boundary, compile-only JNA and JNA Platform ownership by the pinned NeoForge runtime, and the prohibition on a duplicate JNA implementation or native runtime in the mod artifact.

The master plan owns product scope, locked decisions, phase topology, the disallowed-defect boundary, and the exact completion endpoint. This file owns only the dependency-ordered execution of `SEFAUD-PHASE-007`. It may correct ordinary documentation, generated references through their owning generators, evidence indexes, and maintained documentation or evidence validators. It may not add product behavior, change the protected plan set or saved goal, weaken an earlier gate, publish a release, deploy to production, use credentials, or silently repair a product defect that belongs to an earlier phase. A newly discovered product, build, security, command, UI, persistence, integration, compatibility, or Phase 006 verification defect reopens its canonical owner and invalidates closure.

Phase 007 distinguishes three immutable identities:

1. The **Phase 006 verified product revision** is the integrated commit and signed Phase 006 tag whose complete clean-checkout matrix passed.
2. The **release artifact identity** is the exact `build/libs/sef-2.0.0.jar` content, SHA-256, SHA-512, packaged metadata, dependency manifest, and artifact-input digest verified by Phase 006. Documentation-only Phase 007 changes must not alter those inputs or bytes. If a rebuild is not byte-identical, closure stops until the cause is classified and all invalidated Phase 006 proof is rerun.
3. The **Phase 007 closure revision** is the integrated documentation and evidence commit. It must descend contiguously from the Phase 006 integration, contain no product or artifact-input change, describe only the frozen artifact, pass all documentation and final-audit gates, and receive the signed final phase tag after integration.

All final security, dependency, command, UI, persistence, backend, recovery, compatibility, artifact, documentation, and traceability evidence must bind to the same frozen Phase 006 product revision and artifact digest. The Phase 007 closure revision may differ only by owned documentation, bounded evidence summaries, and documentation or evidence validators; it cannot be used to conceal evidence from another product revision. `EXT-001` and `EXT-002` remain separate mandatory prerequisites. If either is unknown, unavailable, partial, failed, or stale, the affected rows and final endpoint remain blocked without converting Linux, macOS, or Windows into an unsupported target and without suppressing, dismissing, or waiving a platform-owned dependency alert.

## Evidence-Based Entry State

| Evidence class | Area | Finding | Source or command | Freshness condition |
|---|---|---|---|---|
| OBSERVED | Phase topology | This is the final mandatory phase. Its canonical owner is Documentation evidence, its canonical requirement is `SEFAUD-REQ-009`, and its phase and external dependencies are `SEFAUD-PHASE-006`, `SEFAUD-REQ-008`, `DEC-009`, `EXT-001`, and `EXT-002`. | `docs/general/plan.md`, Sections 12, 13, 16, and 18 | Invalidated by any authorized master-plan revision, plan-index inconsistency, or incomplete external prerequisite. |
| OBSERVED | Generated references | Command, permission, and configuration references are generated from runtime registries. `generateProjectReferences` writes `docs/COMMAND_REFERENCE.md`, `docs/PERMISSION_REFERENCE.md`, and `docs/CONFIGURATION_REFERENCE.md`; normal tests compare tracked output exactly. | `build.gradle`; `src/test/java/com/enviouse/sef/docs/ProjectDocumentationGenerator.java`; `ProjectDocumentationGeneratorTest`; `ModuleConfigRegistryTest` | Invalidated by generator, registry, catalog, permission, configuration, build-task, or tracked-reference changes. |
| OBSERVED | Performance reference | `generatePerformanceReport` runs the project performance-report generator through the test lifecycle and writes `docs/PERFORMANCE_REPORT.md`. The current file contains historical date, environment, workload, and TPS claims. | `build.gradle`; `docs/PERFORMANCE_REPORT.md` | Invalidated by performance harness, budget, product hot-path, environment, Phase 006 measurement, or report-generation changes. |
| OBSERVED | User and operator documentation | `README.md` is the user entry point. `DOCUMENTATION.md` is the technical and operator reference. Installation, compatibility, migration, security, troubleshooting, test, acceptance, performance, release notes, and release workflow material is distributed through the tracked root documents and `docs/`. | `README.md`; `DOCUMENTATION.md`; Phase 000 documentation inventory; tracked `docs` inventory | Invalidated by a changed tracked documentation set, changed product behavior, or a broken documentation-inventory join. |
| OBSERVED | Current status claims | Current documents contain revision-specific test counts, route counts, artifact sizes and hashes, dates, branch names, open interactive rows, unavailable capabilities, and historical evidence. These are comparison evidence, not Phase 007 proof. | `README.md`; `DOCUMENTATION.md`; `docs/SEF2_ACCEPTANCE.md`; `docs/RELEASE_NOTES.md`; `docs/RELEASE_NOTES_2.0.0.md`; `docs/SECURITY_REVIEW.md`; `docs/COMPATIBILITY_MATRIX.md` | Invalidated immediately for closure unless the claim is bound to the final Phase 007 evidence or explicitly preserved and labeled historical. |
| OBSERVED | Release procedure | `docs/RELEASE_WORKFLOW.md` describes local and GitHub artifact preparation and a separately authorized platform workflow. It explicitly says preparation does not publish automatically, but its branch, status, and acceptance claims must be reconciled with the final candidate. | `docs/RELEASE_WORKFLOW.md` | Invalidated by repository workflow, branch, artifact, version, project mapping, acceptance, or owner-authorization changes. Remote mappings must be freshly verified before any later publication task, not assumed by this phase. |
| OBSERVED | Compatibility boundary | The supported platform is Minecraft 1.21.1, NeoForge 21.1.235, Java 21, Gradle wrapper 8.8 as pinned, NeoForge only, optional universal-client installation, fallback clients, and optional integrations. | `gradle.properties`; Gradle wrapper; `docs/COMPATIBILITY_MATRIX.md`; `docs/INSTALLATION.md`; `DEC-004` | Invalidated by any platform, metadata, dependency, protocol, adapter, or advertised compatibility change. Such a change is outside Phase 007. |
| OBSERVED | Mandatory operating-system support | Linux, macOS, and Windows are all supported Minecraft Java environments under `DEC-009`. A missing disposable environment is an `EXT-001` blocker and never evidence that the operating system is unsupported. | `DEC-009`; `EXT-001`; `SEFAUD-REQ-009`; Phase 006 environment manifests | Invalidated by an unsupported-platform statement, missing operating-system evidence binding, different product revision or artifact, or incomplete `EXT-001` evidence. |
| OBSERVED | Opened-object and native dependency boundary | Security-sensitive writes require provider-specific or native descriptor or handle handling that validates and mutates the same opened object. JNA and JNA Platform are compile-only APIs owned at runtime by pinned NeoForge 21.1.235, and the mod JAR may not contain a duplicate JNA implementation or native runtime. | `DEC-009`; `EXT-001`; `EXT-002`; Phase 001 and Phase 006 packets | Invalidated by path-only wording, missing per-operating-system native proof, runtime provenance or binary-compatibility gaps, embedded duplicate classes or libraries, or a suppressed platform-owned alert. |
| OBSERVED | Migration and recovery guidance | Existing guidance covers modular configuration migration, exact recovery copies, rollback, and prior-JAR retention. It must be reconciled with every migration or rollback result produced by prior phases. | `docs/MIGRATION_GUIDE.md`; `DOCUMENTATION.md`, Sections 17 and 20.6; Phase 002 and Phase 006 evidence | Invalidated by a persistent schema, path, version, backup, rollback, recovery-state, or operator-command change. |
| OBSERVED | Unavailable capability boundary | Sixteen named server-control families are intentionally unavailable and must remain explicit negative contracts, not release capabilities. | `DEC-002`; `README.md`; `DOCUMENTATION.md`, Section 20.3; generated command and permission references; Phase 003 and Phase 006 evidence | Invalidated by handler availability, preview, activation, resolution, diagnostics, UI, generated-reference, or capability-claim changes. Implementing them is excluded. |
| PROPOSED | Final evidence state | The Phase 000 evidence contract supplies a versioned sanitized manifest, stable evidence ids, an approved restricted raw-evidence root outside tracked source, retention rules, and invalidation relations. Phase 007 must consume the actual integrated contract rather than inventing a path or schema. | Phase 000 completion packet and signed Phase 000 tag | Becomes verified only after schema validation and exact digest comparison at Phase 007 entry. |
| PROPOSED | Phase 006 handoff | Phase 006 must provide a complete integrated packet, signed phase tag, frozen product revision, final artifact hashes, full rerun ledger, and no blocked or downgraded mandatory row. | `docs/general/phases/plan-phase-006.md` and its external completion packet when integrated | Entry fails if the file, packet, merge, tag, proof, or final artifact binding is missing, stale, failed, blocked, or inconsistent. |

No planning-time claim is `VERIFIED` for Phase 007 completion. Execution must derive the current facts from the integrated Phase 006 packet, the final clean checkout, live generators, maintained validators, and the frozen artifact.

## Scope Boundaries

### Included Scope

- `SEFAUD-REQ-009`: reconcile `README.md`, `DOCUMENTATION.md`, every affected tracked topic document, generated reference, sanitized evidence record, release-readiness record, and final traceability row with the final implementation and artifact.
- Freeze and validate the exact Phase 006 product revision, artifact-input set, JAR, SHA-256, SHA-512, and closure revision. Prove that Phase 007 changes do not alter product behavior or artifact inputs.
- Reconcile current versus historical claims for test counts, GameTest counts, catalog actions, shortcuts, routes, configuration modules, persistent owners, dependency alerts, runtime results, performance, artifact bytes, hashes, branches, versions, integrations, and open or completed acceptance rows.
- Reconcile every support claim so `README.md`, `DOCUMENTATION.md`, installation, compatibility, security, troubleshooting, acceptance, release, migration, recovery, and all other user or operator documentation identify Linux, macOS, and Windows as supported Minecraft Java environments. Missing `EXT-001` evidence is a blocker, never an unsupported-platform claim.
- Document the provider-specific or native opened-object identity invariant for security-sensitive writes, including same descriptor or handle validation and mutation, safe type and link or reparse checks, stable identity through flush, fail-closed behavior when identity metadata is unavailable, and preservation of prior valid state.
- Document JNA and JNA Platform as compile-only APIs supplied and owned at runtime by pinned NeoForge 21.1.235. Reconcile authoritative provenance, binary compatibility, native loading, artifact inspection, and proof that the mod JAR embeds no duplicate JNA classes, native libraries, implementation, or runtime.
- Carry `EXT-001` and `EXT-002` as distinct prerequisite and evidence rows through documentation, acceptance, traceability, cross-matrix audit, Definition of Done, and endpoint evaluation. Never merge them, clear one with evidence for the other, convert them into a platform exclusion, or suppress a dependency finding.
- Regenerate and compare `docs/COMMAND_REFERENCE.md`, `docs/PERMISSION_REFERENCE.md`, `docs/CONFIGURATION_REFERENCE.md`, and `docs/PERFORMANCE_REPORT.md` only through their owners. Reconcile executable actions and the sixteen intentionally unavailable families with live contracts and final documentation.
- Document exact supported setup, installation, permissions, failure behavior, recovery, migration, rollback, enhanced and fallback client behavior, optional provider present and absent behavior, known limitations, verification commands, expected results, and failure interpretation.
- Validate documentation links, anchors, terminology, generated-reference drift, capability wording, compatibility wording, version claims, evidence ids, artifact hashes, and present-tense completion claims through maintained tests.
- Produce final requirement-to-phase-to-task-to-test-to-runtime-workflow-to-artifact traceability for `SEFAUD-REQ-001` through `SEFAUD-REQ-009` and every registered phase.
- Run the final security, dependency, administrator-command, graphical and textual UI, persistence, backend integration, recovery, compatibility, performance, artifact, documentation, operations, traceability, and release-readiness audit against the same frozen Phase 006 product revision and artifact digest.
- Prove the plan-wide Definition of Done, false-completion protections, stale-evidence rules, and exact owner-selected endpoint.
- Assemble the final completion packet, integrate the final phase through the approved sequential pull-request workflow, verify the resulting candidate revision, create and push the required signed annotated final phase tag, and synchronize repository tracking and wiki state after approved integration.

### Explicit Exclusions

- `FUT-001`: do not implement or activate `admin_journal`, `afk_zones`, `approvals`, `capability_leases`, `chat_channels`, `display_ownership`, `display_profiles`, `player_warp_review`, `portal_policy`, `resource_governor`, `resource_worlds`, `rollouts`, `server_presentation`, `spawn_ecology`, `staff_duty`, or `waypoints`. Preserve their explicit unavailable, unreachable, permission-safe, side-effect-free negative contract.
- `FUT-002`: do not add commands, GUI features, integrations, controls, or gameplay capability while reconciling documentation.
- `FUT-003`: do not modernize or reorganize architecture as part of documentation closure.
- `NG-002`: do not upgrade or broaden Minecraft, NeoForge, Java, Gradle, Parchment, loader, protocol, or optional-integration support.
- `NG-003`: do not publish a GitHub, Modrinth, CurseForge, or other public release; do not deploy; do not mutate production; and do not run destructive verification on production data.
- `NG-004`: do not weaken, skip, waive, relabel, or replace a blocked, failed, stale, manual, multiplayer, graphical, provider, migration, recovery, interruption, or artifact gate with lower-fidelity proof.
- `NG-005`: do not use or retain production credentials, private messages, real addresses, production worlds, personal data, or unrelated user data in documentation, fixtures, evidence, Git, GitHub, or wiki content.
- Do not invoke the release broker, create a marketplace preview, request credentials, create a platform upload, publish a draft release, or treat project mappings as freshly verified release evidence. Those operations require separate explicit owner authority.
- Do not rewrite historical audit or test records to make them appear current. Preserve their original revision and result, add a clear historical label or superseding final record, and link the current evidence.
- Do not modify `docs/general/plan.md`, `plan.index.json`, any registered phase plan, `docs/plan/goal.md`, or another protected planning artifact during execution.
- Do not label Linux, macOS, or Windows unsupported because an `EXT-001` fixture is unavailable. Do not suppress, dismiss, downgrade, or waive a platform-owned dependency alert because `EXT-002` is unavailable, because JNA is platform owned or compile only, or because the mod JAR does not embed the affected library.

## Phase Contract

### SEFAUD-PHASE-007 — Documentation, Evidence, and Final Endpoint Closure

**Objective:** At one final closure revision and one unchanged Phase 006 artifact identity, reconcile all current documentation and sanitized evidence, prove every mandatory requirement and phase at its required fidelity, satisfy every item in the master Definition of Done, and record release readiness without publication.
**Owner:** Documentation evidence
**Dependencies:** SEFAUD-PHASE-006, SEFAUD-REQ-008, DEC-009, EXT-001, EXT-002
**Canonical requirements:** SEFAUD-REQ-009
**Documentation and release impact:** Reconcile `README.md`, `DOCUMENTATION.md`, generated command, permission, configuration, and performance references, all affected compatibility, security, migration, installation, troubleshooting, test, acceptance, and release documents, the sanitized final evidence manifest, the complete traceability report, and the release-readiness record. Publication and deployment remain excluded.
**Next transition:** Final plan-wide completion, only after integrated verification, signed final phase tag, final packet validation, and the exact owner-selected endpoint all pass.

**Entry criteria**

- `SEFAUD-PHASE-006` is complete and integrated through its approved pull request. All required checks passed, and private independent-review capability has either a passing supported result or an explicit optional unsupported disposition. GitHub reports the merge complete, the approved candidate ref contains the merge, and the signed Phase 006 tag verifies.
- The integrated Phase 006 packet identifies one product commit, one source-tree and artifact-input digest, one `build/libs/sef-2.0.0.jar`, SHA-256 and SHA-512 hashes, one dependency manifest, one environment manifest, and one complete final-rerun ledger.
- Every Phase 006 mandatory row is passed at its required fidelity. No row is failed, blocked, incomplete, stale, mocked, or substituted with lower-fidelity proof. No known phase-owned defect remains.
- `EXT-001` is satisfied for the exact frozen product revision and artifact on disposable Linux, macOS, and Windows environments. The packet contains direct server, client, native-writer, filesystem, dependency-loading, shutdown, restart, and applicable recovery evidence for each operating system.
- `EXT-002` is satisfied for the exact frozen product revision and artifact. Authoritative NeoForge 21.1.235 provenance, exact resolved runtime supply, compile-only JNA and JNA Platform binary compatibility and native loading, no-duplicate-runtime artifact inspection, and separate alert closure gates all pass.
- The current candidate still descends from the lineage selected by `DEC-006`; no legacy default-branch content or unrelated user change entered through integration.
- The Phase 000 evidence contract, approved restricted raw-evidence root, inventory set, documentation/claim inventory, traceability schema, sanitization rules, and invalidation graph validate against the signed Phase 000 tag.
- Every earlier signed phase tag, completion packet, integration commit, requirement owner, and phase exit is present and internally consistent. A missing packet or tag is not a documentation correction; it is an upstream gate failure.
- The Phase 007 branch begins from the integrated Phase 006 candidate state. The worktree is classified, unrelated state is preserved, and signing and GitHub access required for the final repository workflow are available.

**Implementation scope**

- Execute all acceptance criteria and required evidence of `SEFAUD-REQ-009` without altering master scope.
- Correct only documentation, generated reference output produced by unchanged owning generators, sanitized evidence summaries, and maintained documentation/evidence validation needed for truthful closure. This work is governed by `SEFAUD-REQ-009`.
- Treat any needed product, source, resource, dependency, schema, protocol, build-input, test-behavior, or runtime-harness change as an upstream reopening. Do not smuggle it into the final documentation phase. This work is governed by `SEFAUD-REQ-009`.
- Preserve the exact Phase 006 release artifact and prove that final documentation work does not alter its input set or byte identity. If an authorized upstream repair is integrated, restart Phase 007 entry from the new complete Phase 006 handoff. This work is governed by `SEFAUD-REQ-009`.

**Execution order**

1. `P007-TASK-001` validates the integrated Phase 006 handoff, every earlier phase packet and signed tag, candidate ancestry, evidence contract, worktree preservation, and exact final product and artifact identities. This advances `SEFAUD-REQ-009`.
2. `P007-TASK-002` freezes the Phase 007 documentation and claim inventory and classifies every current, historical, unavailable, blocked, superseded, and release-readiness statement. This advances `SEFAUD-REQ-009`.
3. `P007-TASK-003` regenerates and reconciles command, permission, configuration, and performance references against their live owners and the frozen artifact inputs. This advances `SEFAUD-REQ-009`.
4. `P007-TASK-004` reconciles `README.md`, `DOCUMENTATION.md`, installation, compatibility, security, migration, recovery, rollback, troubleshooting, performance, and limitations guidance. Every user and operator document identifies Linux, macOS, and Windows as supported Minecraft Java environments and explains opened-object identity, compile-only JNA runtime ownership, no duplicate runtime, and separate `EXT-001` and `EXT-002` blocker handling. This advances `SEFAUD-REQ-009`.
5. `P007-TASK-005` reconciles `test.md`, `audit.md`, `SEFAudit.md` where still referenced, phase test records, `docs/SEF2_ACCEPTANCE.md`, release notes, and release workflow without rewriting historical results as current, narrowing operating-system support, or suppressing platform dependency alerts. This advances `SEFAUD-REQ-009`.
6. `P007-TASK-006` replays every documented Linux, macOS, and Windows setup, permission, diagnostics, failure, recovery, migration, rollback, client-mode, optional-provider, native-writer, dependency, artifact, and evidence command against disposable fixtures and the same frozen product revision and artifact. This advances `SEFAUD-REQ-009`.
7. `P007-TASK-007` validates and finalizes the sanitized evidence manifest, raw-evidence routing, privacy classification, retention, artifact binding, and evidence invalidation closure. This advances `SEFAUD-REQ-009`.
8. `P007-TASK-008` implements or updates maintained documentation link, anchor, terminology, claim, generated-drift, version, count, unavailable-capability, and artifact-hash tests, then fixes every failure in owned documentation. This advances `SEFAUD-REQ-009`.
9. `P007-TASK-009` produces and validates final requirement-to-phase-to-prerequisite-to-task-to-test-to-workflow-to-artifact traceability, retaining distinct `EXT-001` and `EXT-002` rows and the common frozen revision and artifact identity, with no orphan, duplicate, circular, missing, or lower-fidelity row. This advances `SEFAUD-REQ-009`.
10. `P007-TASK-010` runs the final security, dependency, administrator-command, graphical and textual UI, persistence, backend integration, recovery, compatibility, performance, artifact, documentation, operations, traceability, and release-readiness cross-matrix audit on the same frozen Phase 006 product revision and artifact digest. This advances `SEFAUD-REQ-009`.
11. `P007-TASK-011` runs the false-completion and stale-evidence audit and proves all seventeen master Definition of Done conditions at the same identities, including the operating-system support, opened-object, compile-only JNA, no-duplicate-runtime, and external-prerequisite clauses. This advances `SEFAUD-REQ-009`.
12. `P007-TASK-012` performs the final clean-checkout documentation, reference, build, artifact-identity, secret, privacy, path, and complete-diff verification in the prescribed order. This advances `SEFAUD-REQ-009`.
13. `P007-TASK-013` assembles the final completion packet, preflights private independent-review capability, obtains one review when supported or records the optional unsupported disposition, resolves every received actionable finding, and freezes the ready Phase 007 branch. This advances `SEFAUD-REQ-009`.
14. `P007-TASK-014` commits and pushes the phase, opens or updates the final phase pull request, waits for all required checks, and merges through GitHub with the required merge method. No check is bypassed and no direct update is made to the integration target. This advances `SEFAUD-REQ-009`.
15. `P007-TASK-015` verifies the integrated closure revision from a clean checkout, proves artifact identity and packet consistency again, creates and pushes the signed annotated final phase tag, and only then synchronizes final issue, pull request, Project, milestone, release-readiness, and wiki state without publishing a release. This advances `SEFAUD-REQ-009`.
16. `P007-TASK-016` validates the exact owner-selected endpoint and emits the final plan-completion result. Any failed predicate returns to its canonical task or reopens its upstream phase; it never produces qualified success. This advances `SEFAUD-REQ-009`.

**Required evidence**

- Verified Phase 000 through Phase 006 packets, merge commits, signed phase tags, tag signature results, and contiguous ancestry.
- Exact product commit, closure commit, artifact-input digest, JAR path, byte size, SHA-256, SHA-512, entry inventory, dependency manifest, generated-reference digests, and proof that Phase 007 did not change artifact inputs.
- Zero-drift generated command, permission, configuration, and performance references tied to the final product identity.
- Documentation inventory with each claim's owner, evidence id, revision, artifact, status, fidelity, invalidation set, and final disposition.
- Replayed Linux, macOS, and Windows user and operator procedures covering setup, permission, failures, recovery, migration, rollback, enhanced and fallback clients, optional providers, native opened-object writing, runtime dependency loading, known limitations, evidence generation, and artifact inspection.
- Separate complete `EXT-001` and `EXT-002` evidence records, with `EXT-001` bound to the three-operating-system runtime matrix and `EXT-002` bound to authoritative platform provenance and alert closure. Neither record may be replaced by an unsupported-platform statement, dependency-alert suppression, packaged-artifact-only inference, or evidence from a different revision.
- Maintained documentation link, anchor, terminology, claim, count, version, capability, generated-drift, secret, personal-data, absolute-path, and artifact-hash test results.
- Final requirement-to-phase-to-task-to-test-to-runtime-workflow-to-artifact traceability report.
- Final security, dependency, command, UI, persistence, backend, recovery, artifact, documentation, operations, and traceability cross-matrix audit, false-completion audit, stale-evidence audit, seventeen-item Definition of Done checklist, release-readiness record, and exact endpoint evaluation, all bound to the same frozen product revision and artifact digest.
- Final pull request checks, private independent-review capability and result disposition, merge proof, post-merge clean verification, signed final phase tag, repository tracking synchronization, and post-merge wiki parity proof.

**Exit criteria**

- Every acceptance criterion and required evidence item of `SEFAUD-REQ-009` passes.
- `README.md`, `DOCUMENTATION.md`, generated references, compatibility, security, migration, installation, troubleshooting, test, acceptance, performance, release notes, and release workflow describe only behavior proven for the frozen product and artifact; historical material is unmistakably historical.
- Every user and operator document names Linux, macOS, and Windows as supported Minecraft Java environments. No missing or blocked environment is expressed as unsupported, optional, or excluded.
- User and operator documentation describes provider-specific or native opened-object identity for security-sensitive writes, same descriptor or handle validation and mutation through flush, unsafe link or reparse rejection, and fail-closed recovery. It states that JNA and JNA Platform are compile-only APIs supplied at runtime by pinned NeoForge 21.1.235 and that the mod artifact contains no duplicate JNA classes, native libraries, implementation, or runtime.
- `EXT-001` and `EXT-002` are both satisfied and remain separately visible in acceptance, evidence, traceability, dependency, security, compatibility, and release-readiness records. No platform-owned alert is suppressed or waived.
- Generated command, permission, configuration, and performance references match their final live contracts exactly. Executable versus unavailable status is accurate, and none of the sixteen unavailable families is advertised as implemented, active, or release capability.
- Every documented command and operator procedure has been replayed or is explicitly classified as a nonexecuting informational step. Setup, permission, failure, recovery, migration, rollback, client, provider, limitation, and evidence behavior matches the artifact.
- The sanitized evidence manifest links every requirement and mandatory matrix row to the final identities, environment, exact proof, expected and actual result, fidelity, and raw-evidence id. It contains no secret, personal data, private host path, production content, or unbounded log.
- Final traceability contains exactly one canonical owner for every mandatory requirement and row and no missing phase, external prerequisite, task, test, runtime workflow, artifact, documentation obligation, or release-readiness gate.
- The final cross-matrix audit finds no known disallowed defect and proves that security, dependency, command, UI, persistence, backend, recovery, artifact, documentation, operations, and traceability evidence share the frozen Phase 006 product revision and artifact digest. Every master Definition of Done item passes without qualification.
- The final phase is merged through the approved pull request, the integration result is reverified from a clean checkout, the signed annotated final phase tag verifies, repository tracking is synchronized, and the wiki is updated only with integrated facts.
- Release readiness is recorded. No release, marketplace preview, publication, deployment, production mutation, credential access, or destructive production verification occurred.
- The final phase verifies the owner-selected completion endpoint and plan-wide Definition of Done.
- No known mandatory phase-owned defect remains.

## Inputs and Upstream Contracts

| Input or contract | Provider | Required state | Validation | Failure behavior |
|---|---|---|---|---|
| Master plan, plan index, registered phase set, and saved goal | Protected planning artifacts | Internally consistent and immutable where required; the saved goal retains its creation-time Phase 000 handoff, while integrated phase evidence identifies `SEFAUD-PHASE-007` as the current contiguous phase and `SEFAUD-REQ-009` as canonical | Read all registered files through EOF; validate index, digests, contiguous numbering, saved-goal integrity, prior phase integration, and current-phase derivation without refreshing the goal | Stop on protected-artifact conflict. Do not edit, refresh, or rebind the goal or plan. |
| Phase 000 evidence contract and baseline | `SEFAUD-PHASE-000` | Versioned schemas, stable ids, approved restricted raw-evidence root, sanitized manifest rules, complete inventories, documentation claim inventory, invalidation graph, integrated commit, and signed tag | Validate packet schema and digests; regenerate maintained inventories where prescribed | Reopen the affected Phase 000 gate if ownership, schema, evidence route, or inventory is missing or stale. |
| Security and dependency closure | `SEFAUD-PHASE-001` | Every trust boundary and dependency alert has a final disposition; no prohibited security or applicable critical/high finding remains | Verify packet, findings, scans, dependency graph, artifact inspection, merge, and signed tag | Reopen Phase 001 for any unresolved or stale security, privacy, secret, permission, filesystem, dependency, or supply-chain row. |
| Persistence closure | `SEFAUD-PHASE-002` | Every durable owner and cross-store invariant has passed corruption, concurrency, interruption, migration, recovery, rollback, lifecycle, retention, and privacy proof | Verify packet, durable-owner join, recovery artifacts, runtime workflows, merge, and signed tag | Reopen Phase 002 for missing, stale, failed, or contradictory persistence proof. Do not correct product behavior in docs. |
| Command closure | `SEFAUD-PHASE-003` | Every executable administrative action passes its universal row and all sixteen unavailable families pass their negative contract | Verify action identities, matrix, generated references, runtime proof, feedback and audit evidence, merge, and signed tag | Reopen Phase 003 for an orphan action, stale count, executable defect, policy mismatch, or unavailable-family misrepresentation. |
| UI closure | `SEFAUD-PHASE-004` | Every graphical and textual operator surface passes layout, input, accessibility, privacy, state, revocation, and fallback evidence | Verify UI ledger, screenshots/recordings by sanitized evidence id, runtime equivalence, merge, and signed tag | Reopen Phase 004 for missing required graphical proof or any blocking/materially misleading UI defect. |
| Backend integration closure | `SEFAUD-PHASE-005` | Lifecycle, cross-channel, provider, reconnect, dimension, partial-failure, shutdown, classloading, thread, and audit matrices pass | Verify packet, runtime traces, integration rows, merge, and signed tag | Reopen Phase 005 for stale or failed mandatory integration proof. |
| Final runtime and artifact verification | `SEFAUD-PHASE-006` | Complete clean-checkout matrix passes after the last product change; one exact product revision and artifact are frozen; no mandatory row is blocked or downgraded | Validate ledger, environment, commands, reports, JAR hashes and entries, artifact inputs, diff, integration commit, and signed tag | Stop Phase 007. Reopen Phase 006 or the canonical earlier owner. Never proceed with partial closure. |
| Cross-platform runtime prerequisite | `EXT-001`, `DEC-009`, Phase 006 | Complete disposable Linux, macOS, and Windows Java 21 and NeoForge 21.1.235 evidence for the exact frozen product revision and artifact, including server, client, native writer, filesystem, shutdown, restart, dependency loading, and recovery rows | Validate all three environment manifests, revision and artifact foreign keys, direct native workflows, expected and actual results, raw-evidence digests, and no-production-data controls | Keep `EXT-001`, every affected row, this phase, and the endpoint blocked. Preserve all three operating systems as supported Minecraft Java targets. |
| Platform dependency prerequisite | `EXT-002`, `DEC-009`, Phase 001 and Phase 006 | Authoritative NeoForge 21.1.235 artifact provenance and runtime supply, compile-only JNA and JNA Platform compatibility and native loading on all three operating systems, no duplicate runtime in the mod JAR, and separate closure of every platform-owned alert gate | Validate authoritative coordinates, origins, versions, hashes, resolved compile and runtime paths, binary compatibility, native loading, JAR entries, candidate graph, installed runtime, reachability, advisory applicability, and compatible remediation | Keep `EXT-002`, dependency and security closure, this phase, and the endpoint blocked. Do not suppress an alert, infer closure from platform ownership or mod-JAR absence, or narrow operating-system support. |
| Pinned compatibility contract | `DEC-004`, build metadata, Phase 006 | Minecraft 1.21.1, NeoForge 21.1.235, Java 21, checked-in wrapper, Parchment 2024.11.17, NeoForge only, current optional protocol and integrations | Compare build files, metadata, dependency manifest, artifact metadata, and docs | Any drift is out of Phase 007 scope and invalidates compatibility, runtime, artifact, and release evidence. |
| Final tracked documentation inventory | Phase 000 inventory plus repository at Phase 006 integration | Every tracked user, operator, technical, test, acceptance, security, compatibility, migration, and release document has an owner and claim set | Exact path-set comparison and link graph; classify historical versus current | Missing or unowned docs block reconciliation. A changed path set reopens Phase 000 drift ownership. |
| Approved candidate lineage and repository workflow | `DEC-006`, integrated Phase 006 state, repository rules | Contiguous candidate ancestry, clean phase base, required signing, checks, merge method, and no unrelated user state | Git ancestry, status, signing, remote, branch, and rules checks | Stop without reset, rebase, direct push, or history rewrite. Owner resolves ambiguous integration authority. |

## Outputs and Downstream Contracts

| Output or contract | Consumer | Guaranteed state | Compatibility or versioning | Evidence |
|---|---|---|---|---|
| Final reconciled documentation set | Users, administrators, maintainers, support, later release operator | Current behavior, commands, permissions, configuration, compatibility, setup, failure, recovery, migration, rollback, client/provider behavior, limitations, and verification are truthful for the frozen artifact. Every user and operator document identifies Linux, macOS, and Windows as supported Minecraft Java environments and accurately describes the opened-object and native dependency boundary. | Preserves `DEC-004`, `DEC-009`, stable public ids, schemas, protocol, unavailable-family contract, separate `EXT-001` and `EXT-002` status, compile-only JNA ownership, and no-duplicate-runtime rule | Documentation validators, cross-platform operator-procedure replay, support-claim checks, claim inventory, final diff |
| Final generated references | Users, administrators, runtime contract | Command, permission, configuration, and performance output exactly matches live owners with stable ordering and zero unexplained drift | Generated files remain owned by existing generators and their schemas | Two clean generations, exact comparison, generator tests, live-contract join |
| Final sanitized evidence manifest | Plan completion audit, maintainers, later release operator | Every mandatory row links to final identities, environment, proof, result, fidelity, invalidation, and approved raw-evidence id without sensitive material | Uses Phase 000 schema and stable evidence ids; no private absolute paths | Schema validation, digest check, sanitizer and secret/privacy scans |
| Final traceability report | Plan completion audit | `SEFAUD-REQ-001` through `SEFAUD-REQ-009` map through phase, distinct external prerequisite, task, test, runtime workflow, artifact, documentation, and result with no orphan or duplicate, all at the same frozen product revision and artifact identity | Stable ids from master and phase plans remain unchanged; `EXT-001` and `EXT-002` remain separate | Exact-set and foreign-key validators, frozen-identity join, human exception review |
| Final release-readiness record | Owner and separately authorized future publisher | Artifact and supporting evidence are ready under the selected endpoint; publication remains unauthorized and unperformed | Version lineage remains 2.0.0 and target platform remains frozen | Artifact hashes, Definition of Done, no-publication statement, packet digest |
| Final completion packet | Owner, repository maintainers, future support | Reviewable plan-wide proof, integration state, final tag, rollback point, limitations, and evidence routing | Packet lives outside protected plan set; raw evidence remains restricted | Packet schema, final audit, post-merge verification, tag signature |
| Integrated Phase 007 closure and signed final tag | Repository history and owner | Approved merge contains only final phase documentation/evidence/tooling scope, descends from Phase 006, and binds the completion packet and release artifact | No product or artifact-input drift; no direct integration-target push | PR checks, merge commit, clean post-merge replay, signed annotated tag |
| Final GitHub and wiki synchronization | Repository collaborators and operators | Issue, pull request, Project, milestone, and wiki state reflect only integrated facts and release readiness, not publication | Tracked docs remain canonical; wiki is a curated integrated view | Read-back of remote state and wiki links after merge |
| Exact endpoint decision | Owner | One unqualified `complete` result only if every endpoint predicate passes; otherwise one explicit blocker with canonical owner and invalidated evidence | Uses `DEC-003` and master Section 18 verbatim | Endpoint validator and signed completion packet |

## Work Packages

| Task ID | Requirement IDs | Work | Inputs and dependencies | Outputs | Affected components or interfaces | Verification |
|---|---|---|---|---|---|---|
| `P007-TASK-001` | `SEFAUD-REQ-009`, `SEFAUD-REQ-008`, `DEC-006`, `DEC-009`, `EXT-001`, `EXT-002` | Validate every upstream phase integration and tag, separately validate both external prerequisites, then freeze the product revision, artifact inputs, JAR identity, evidence schema, documentation inventory, Phase 007 branch base, and unrelated-state preservation. | Phase 000 through Phase 006 packets and signed tags; complete `EXT-001` and `EXT-002` packets; entry criteria | Phase 007 entry manifest and immutable identity record | Git metadata; phase packets; three-operating-system environment manifests; platform provenance and advisory ledger; approved external evidence root; `build/libs/sef-2.0.0.jar` | Ancestry, tag signatures, packet digests, all three operating-system manifests, separate external-prerequisite dispositions, artifact hashes, path-set digest, worktree classification, and source/artifact-input digest all match the same frozen identity. |
| `P007-TASK-002` | `SEFAUD-REQ-009`, `DEC-009` | Derive the final documentation claim ledger. Assign each prose statement, table row, count, hash, version, command, capability, operating-system support statement, native-writer statement, JNA ownership statement, dependency-alert disposition, limitation, link, status, and release assertion a source owner, current/historical class, evidence id, invalidation set, and required disposition. | Task 001; Phase 000 documentation inventory; `EXT-001`; `EXT-002` | Complete final claim ledger and owned correction set | `README.md`; `DOCUMENTATION.md`; `sef2.md`; `audit.md`; `test.md`; `SEFAudit.md`; `PORTING_NOTES.md`; tracked `docs` set | Exact document and claim inventory has zero missing owner, duplicate current authority, unlabeled historical result, unsupported claim, unsupported-platform claim, suppressed dependency alert, merged prerequisite, or dead evidence id. |
| `P007-TASK-003` | `SEFAUD-REQ-009`, `DEC-002`, `DEC-004` | Run existing generators from a clean checkout, compare output to tracked references and live registries, reconcile executable and unavailable capability status, and repeat generation to prove determinism. Never hand-edit generated files. | Tasks 001 and 002; Phase 006 artifact inputs | Zero-drift generated reference set and comparison report | `docs/COMMAND_REFERENCE.md`; `docs/PERMISSION_REFERENCE.md`; `docs/CONFIGURATION_REFERENCE.md`; `docs/PERFORMANCE_REPORT.md`; existing generators and tests; `build.gradle` | `./gradlew generateProjectReferences generatePerformanceReport`; focused generator and registry tests; two normalized output digests; live identity and unavailable-family exact-set join. |
| `P007-TASK-004` | `SEFAUD-REQ-009`, `DEC-004`, `DEC-005`, `DEC-008`, `DEC-009`, `EXT-001`, `EXT-002` | Reconcile all user, technical, and operator guidance. Every applicable document must identify Linux, macOS, and Windows as supported Minecraft Java environments, describe provider-specific or native same-opened-object identity and fail-closed recovery, state compile-only JNA and JNA Platform ownership by pinned NeoForge with no duplicate runtime, and retain separate external-prerequisite blockers. Name exact commands, prerequisites, expected results, failure interpretation, safe recovery, and point-of-no-return status. | Tasks 002 and 003; earlier phase packets; complete `EXT-001` and `EXT-002` evidence | Truthful current user and operator documentation | `README.md`; `DOCUMENTATION.md`; `docs/INSTALLATION.md`; `docs/COMPATIBILITY_MATRIX.md`; `docs/MIGRATION_GUIDE.md`; `docs/SECURITY_REVIEW.md`; `docs/PERFORMANCE_REPORT.md`; every affected user or operator document | Claim-ledger closure, exact three-operating-system support-set check, opened-object and native dependency terminology checks, link checks, operator replay, security/privacy review, compatibility exact-set comparison, and no stale count/hash/version. |
| `P007-TASK-005` | `SEFAUD-REQ-009`, `DEC-001`, `DEC-003`, `DEC-009`, `EXT-001`, `EXT-002` | Reconcile test, audit, acceptance, release notes, and release workflow records. Preserve historical evidence with its original revision; add or update a final current record tied to the frozen product and closure identities; remove false open or complete assertions while retaining separate external blockers and dependency dispositions. | Tasks 002 through 004; all phase results | Final current acceptance and release-readiness documentation | `test.md`; `audit.md`; `SEFAudit.md` where referenced; `docs/PHASE_*_TESTS.md`; `docs/SEF2_ACCEPTANCE.md`; `docs/RELEASE_NOTES.md`; `docs/RELEASE_NOTES_2.0.0.md`; `docs/RELEASE_WORKFLOW.md` | Current record exact-set matches final manifest; historical records remain labeled; no publication language, stale branch, stale artifact, stale count, blocked-as-pass, unsupported-platform claim, merged `EXT-001` and `EXT-002` state, or suppressed alert remains. |
| `P007-TASK-006` | `SEFAUD-REQ-009`, `DEC-004`, `DEC-005`, `DEC-009`, `EXT-001`, `EXT-002` | Replay the documented installation, update, configuration, permission, diagnostics, failure, recovery, migration, rollback, enhanced-client, fallback-client, incompatible-client, provider-present, provider-absent, opened-object native-writer, runtime dependency-loading, artifact-inspection, and evidence-generation procedures on disposable Linux, macOS, and Windows fixtures. Record expected and actual results. | Tasks 003 through 005; Phase 006 environments and artifact; complete external-prerequisite fixtures | Cross-platform operator procedure replay ledger and corrections | Commands and runbooks in `README.md`, `DOCUMENTATION.md`, `docs/INSTALLATION.md`, `docs/MIGRATION_GUIDE.md`, `docs/COMPATIBILITY_MATRIX.md`, `docs/RELEASE_WORKFLOW.md`, `test.md` | Every executable step succeeds as documented or produces its documented safe failure at the same frozen product revision and artifact digest. Opened-object identity and JNA runtime ownership match the platform evidence. Destructive cases use disposable roots; rollback restores exact baseline; no production or credential path is touched. |
| `P007-TASK-007` | `SEFAUD-REQ-009`, `DEC-005`, `DEC-009`, `EXT-001`, `EXT-002` | Join every mandatory row, including each separate external prerequisite row, to final evidence; validate common frozen-revision and artifact foreign keys, raw-evidence digests and access routing, sanitize bounded summaries, classify retention and privacy, remove or quarantine unsafe copies, and close all invalidation edges. | Tasks 001 and 006; Phase 000 evidence contract; all phase packets; both external-prerequisite packets | Final sanitized evidence manifest and raw-evidence index | Approved restricted external evidence root; tracked bounded summaries; cross-platform environment evidence; platform dependency provenance and advisory evidence; evidence schema and sanitizer | Schema, foreign-key, common frozen-identity, distinct-prerequisite, digest, secret, personal-data, host-path, size, retention, and access tests pass. No raw log, recording, profile, corrupted fixture, or world snapshot enters tracked source. |
| `P007-TASK-008` | `SEFAUD-REQ-009`, `DEC-002`, `DEC-004`, `DEC-009`, `EXT-001`, `EXT-002` | Maintain automated documentation quality gates for path existence, relative links, anchors, generated ownership, terminology, versions, counts, artifact hashes, current/historical status, unavailable features, three-operating-system support, opened-object semantics, compile-only JNA ownership, duplicate-runtime absence, separate prerequisite status, platform-alert closure, release wording, and evidence ids. Add synthetic false-claim and stale-evidence fixtures where existing gates do not cover the final contract. | Tasks 002 through 007 | Passing documentation and claim validation suite | Existing documentation generator tests and Phase 000 claim/drift validators; `build.gradle`; tracked documentation | `./gradlew check`; focused documentation tests; injected broken link, stale count, wrong hash, unsupported version, unsupported-Windows claim, path-only-writer claim, runtime-JNA-ownership error, duplicate-runtime omission, merged external blocker, suppressed platform alert, unavailable-as-feature, blocked-as-pass, and private-path fixtures all fail as expected. |
| `P007-TASK-009` | `SEFAUD-REQ-001` through `SEFAUD-REQ-009`, `EXT-001`, `EXT-002` | Generate final exact traceability from each master requirement and acceptance criterion through canonical phase, distinct external prerequisite, stable task, inventory row, finding disposition, automated test, required real workflow, artifact/evidence id, documentation obligation, and result. Require every row to bind to the same frozen product revision and artifact digest. | Tasks 001, 007, and 008; all registered phase plans; both external-prerequisite packets | Final plan-wide traceability report | Master and phase stable ids; external prerequisite ids; Phase 000 inventories; all completion packets and manifests | Exact-set, uniqueness, acyclic dependency, valid foreign-key, fidelity, common frozen-identity, distinct external-prerequisite, and no-orphan validators pass; every exception is reviewed and resolved. |
| `P007-TASK-010` | `SEFAUD-REQ-002` through `SEFAUD-REQ-009`, `DEC-003`, `DEC-009`, `EXT-001`, `EXT-002` | Conduct one integrated final audit across security/privacy, platform and candidate dependencies, administrator commands, graphical and textual UI, persistence, backend lifecycle/integration, recovery and rollback, compatibility, performance, artifact, documentation, operations, traceability, and release readiness. Cross-check equivalent channels, shared identities, operating-system claims, opened-object guarantees, JNA ownership, and separate alert gates. | Tasks 006 through 009; all prior phase matrices; both external-prerequisite packets | Final cross-matrix audit and finding register | All phase matrices; final JAR; resolved runtime graphs; docs; generated references; evidence manifest; final traceability report | Every domain has complete evidence bound to the same frozen product revision and artifact digest. There is zero missing mandatory row, cross-domain identity mismatch, unexplained contradiction, disallowed known defect, capability mismatch, unsupported-platform claim, suppressed platform alert, duplicate runtime, or release gate. Any finding is routed to its canonical owner and closure stops. |
| `P007-TASK-011` | `SEFAUD-REQ-009`, `DEC-003`, `DEC-009`, `EXT-001`, `EXT-002`, `NG-004`, `NG-006` | Execute false-completion and stale-evidence tests, then evaluate each of the seventeen master Definition of Done clauses. Confirm every proof shares the correct product, artifact, environment, and closure identity and required fidelity, and that both external prerequisites pass separately. | Tasks 009 and 010 | Signed Definition of Done result and stale-evidence report | Master Section 18; evidence invalidation graph; final traceability; claim ledger | Injected blocked, stale, mocked, lower-fidelity, wrong-commit, wrong-artifact, unavailable-fixture, single-operating-system, unsupported-platform, path-only-writer, duplicate-native-runtime, suppressed-alert, merged-prerequisite, missing-row, and post-proof-change cases all prevent completion. All real clauses pass. |
| `P007-TASK-012` | `SEFAUD-REQ-008`, `SEFAUD-REQ-009`, `DEC-009`, `EXT-001`, `EXT-002` | From a clean checkout after the last owned change, run final docs/reference/check/build commands, rebuild and inspect the JAR, prove byte and input identity to Phase 006, revalidate authoritative runtime dependency ownership and no duplicate runtime, scan docs/evidence/diff, and rerun every invalidated row in order. | Tasks 003 through 011 | Final clean verification ledger and frozen closure candidate | Checked-in wrapper; Java 21; generated references; `build/libs/sef-2.0.0.jar`; resolved runtime manifests; tracked diff; approved evidence root | Required commands pass; generated drift is zero; JAR hashes and entries match; compile-only JNA ownership remains valid; no duplicate JNA class or native runtime and no source/resource/build-input drift, secret, personal data, private path, cache, log, world, or unrelated file appears. Every final matrix and traceability result names the same frozen product and artifact identities. |
| `P007-TASK-013` | `SEFAUD-REQ-009`, `DEC-009`, `EXT-001`, `EXT-002` | Assemble the final completion packet, review every diff and evidence exception, verify separate external-prerequisite closure and common frozen identities, preflight private independent-review capability, request one review when supported or record the optional unsupported disposition, address received valid findings, and freeze the ready branch. | Task 012; repository review contract | Review-complete final packet and ready final phase branch | Git diff; evidence bundle; traceability; documentation; external-prerequisite packets; issue and Project state | Packet schema and all final validators pass after last change; supported-review findings are resolved with evidence; no protected plan, goal, product, or artifact-input change, unsupported-platform claim, suppressed alert, or prerequisite conflation exists. |
| `P007-TASK-014` | `SEFAUD-REQ-009` | Commit and push with required author and signing, open or update the Phase 007 pull request, wait for every required check and resolved conversation, enable approved auto-merge if appropriate, and merge through GitHub using the required merge-commit method. | Task 013; repository branch and pull-request rules | Integrated Phase 007 closure revision | Git and GitHub repository workflow | Commit signature, PR target, checks, review, conversation state, merge method, and merged result verify. No direct push, bypass, squash, rebase, or publication occurs. |
| `P007-TASK-015` | `SEFAUD-REQ-009`, `DEC-009`, `EXT-001`, `EXT-002` | From a clean checkout of the merged result, rerun nonmutating closure checks, compare packet and artifact identities, revalidate all three supported operating-system claims and separate prerequisite records, create and push the signed annotated final phase tag, then synchronize issue, PR, Project, milestone, release-readiness, and wiki state with integrated facts. | Task 014 | Signed final phase tag and synchronized repository state | Integrated candidate ref; GitHub Issues, PR, Project, milestone, wiki; tracked docs as canonical | Post-merge checks pass; tag points at merged closure commit and verifies; remote state reads back correctly; wiki identifies Linux, macOS, and Windows as supported and links current integrated docs; no alert is suppressed and no release object or upload is created. |
| `P007-TASK-016` | `SEFAUD-REQ-001` through `SEFAUD-REQ-009`, `DEC-003`, `DEC-009`, `EXT-001`, `EXT-002` | Evaluate the exact owner-selected endpoint as a deterministic conjunction over all mandatory requirements, phases, separate external prerequisites, defects, cross-platform runtime proof, opened-object proof, native dependency ownership, documentation, artifact, integration, and no-publication predicates. Emit complete only if every predicate passes. | Task 015; final packet and signed tag | Final plan-completion record or exact blocker | Completion packet and endpoint validator | Result quotes the endpoint, names final commits, artifact hashes and tag, and has no qualifier. A false or unknown `EXT-001` or `EXT-002` predicate remains separately named and leaves the plan incomplete without narrowing supported operating systems or suppressing an alert. |

### Ordering, Failure Handling, and Safe Parallelism

- Task 001 is a hard gate. No documentation edit may begin while the Phase 006 merge, signed tag, complete matrix, artifact identity, evidence schema, ancestry, workspace boundary, `EXT-001`, or `EXT-002` is unresolved. The two external prerequisites are validated separately.
- Task 002 must complete before prose edits so current claims are not corrected by search-and-replace without evidence ownership.
- Task 003 may run concurrently with the read-only portion of Task 002 after identities are frozen. Generated files remain generator-owned; manual edits are prohibited.
- Tasks 004 and 005 may proceed in parallel by nonoverlapping paths after Tasks 002 and 003 define the final claims. One task owns a path at a time. Cross-links and duplicate authority are reconciled before Task 006.
- Task 006 is sequential by environment when fixtures share a server, client, provider, world, configuration, migration root, or evidence writer. Separate read-only link and claim checks may run concurrently.
- Tasks 007 and 008 may run in parallel only after the replay ledger is stable. Task 007 owns evidence contents; Task 008 owns validators. Neither may rewrite a failed result into success.
- Tasks 009 through 016 are sequential final gates. Any edit after Task 009 invalidates affected traceability and claim checks. Any edit after Task 012 invalidates the final clean verification and packet. Any post-merge repository-content change requires a new integration cycle before tagging.
- A documentation-only correction invalidates its claim row, links, terminology, generated-reference comparison where relevant, operator replay where it changes a step, final traceability, final audit, Definition of Done, diff audit, and packet. It does not automatically invalidate unrelated runtime proof when the evidence graph proves no behavioral or artifact-input intersection.
- A source, resource, build, dependency, schema, protocol, test-behavior, runtime-harness, generator-semantic, or artifact-input change invalidates the Phase 006 freeze. Stop Phase 007, route the change to its canonical earlier phase, integrate it sequentially, rerun Phase 006 completely after the last product change, and restart Task 001 from the new handoff.
- A required fixture or remote capability that is unavailable remains blocked. An unavailable Linux, macOS, or Windows environment remains an `EXT-001` blocker, not an unsupported-platform declaration. Unavailable authoritative dependency evidence remains an `EXT-002` blocker, not a suppressed alert. Do not replace either with prose, screenshots from another revision, mocks, headless startup, packaged-mod-only inspection, or static inference.
- A failed command records exact expected versus actual behavior and a bounded sanitized decisive error. A successful retry receives a new attempt id, links the failed attempt, and states why the evidence became valid. Failure history is never erased.

## Architecture and Implementation Boundaries

### Documentation Ownership and Dependency Direction

Runtime registries, build metadata, schemas, code, tests, real workflows, and the final artifact are upstream authority. Generated references and ordinary documentation consume those facts. Documentation never becomes independent runtime authority. The final dependency direction is:

```text
integrated product and live contracts
  -> maintained tests and real workflows
  -> sanitized evidence and artifact identities
  -> generated references and ordinary documentation
  -> final traceability and release-readiness record
  -> post-merge wiki navigation
```

`README.md` remains the user entry point. `DOCUMENTATION.md` remains the maintainer and operator architecture reference. The tracked `docs` set owns focused installation, migration, compatibility, security, performance, feature, test, acceptance, release-note, and release-workflow detail according to the Phase 000 documentation inventory. Duplicate current authorities are prohibited. Historical audit and test documents may remain, but each historical section must retain its evidence revision and cannot override the final current ledger.

### Generated Content

`docs/COMMAND_REFERENCE.md`, `docs/PERMISSION_REFERENCE.md`, and `docs/CONFIGURATION_REFERENCE.md` are owned by `ProjectDocumentationGenerator` and `ModuleConfigRegistry`; `generateProjectReferences` is the only editing route. `docs/PERFORMANCE_REPORT.md` is owned by the performance report generator; `generatePerformanceReport` is the only editing route. Existing exact-drift tests remain mandatory. Phase 007 may improve validators or presentation only when the change does not alter live behavior or artifact inputs. A semantic generator change that changes represented contracts reopens the relevant upstream phase and Phase 006.

Generated references must distinguish executable action, discoverable schema, command fallback, and intentionally unavailable runtime. A schema or permission appearing in a generated file does not prove executable behavior. The sixteen families in `DEC-002` must be labeled consistently across diagnostics, UI, generated references, README, technical documentation, acceptance, and release notes.

### Evidence Model and Privacy

The Phase 000 evidence schema, stable ids, sanitization rules, invalidation graph, and approved restricted external root remain authoritative. Do not invent a second evidence directory or schema. The final tracked manifest is a bounded sanitized index. Raw logs, screenshots, recordings, profiles, dependency reports, corrupted fixtures, migration snapshots, world snapshots, and full runtime traces remain outside tracked source under restricted access.

Each final evidence record must include stable row id, requirement id, phase id, task id, test or workflow id, evidence class, required fidelity, result, expected result, actual result, product commit, closure commit where applicable, artifact SHA-256 and SHA-512 where applicable, environment id, fixture id, start and finish time, command or procedure id, bounded sanitized diagnostic, raw-evidence relative id and digest, invalidation inputs, prior attempt link, and reviewer disposition. It must not include credential values, userinfo-bearing URLs, real player identities, private messages, addresses, production paths, host home paths, production world data, or unbounded output.

The final manifest must expose `EXT-001` and `EXT-002` as distinct stable prerequisite records with independent result, evidence, invalidation, and blocker fields. Every security, dependency, command, UI, persistence, backend, recovery, artifact, documentation, operations, and traceability record must foreign-key to the same frozen Phase 006 product commit and artifact hashes. A record tied only to the later documentation closure commit must also name the unchanged frozen product and artifact; it cannot substitute that closure commit for runtime identity.

If unsafe evidence is detected, stop propagation, quarantine it in the approved restricted root, record only the exposure class and affected evidence ids, invalidate every packet containing it, and request owner-led credential rotation if a real secret may have been exposed. Recreate proof with synthetic data. Do not store the secret or raw unsafe text in the remediation record.

### Configuration, Permissions, and Public Identifiers

Documentation must preserve exact configuration keys, permission ids, command routes, action ids, resource locations, schema versions, serialized fields, network versions, and optional-integration identities from the frozen product. Never normalize or rename literals for prose style. Every permission example must state its exact effect, default, actor/source scope, target scope, hierarchy or exemption rule, and dangerous wildcard implications where applicable. Generated references remain the exhaustive literal source.

Migration and rollback documentation must use actual current paths and commands from the frozen implementation and earlier persistence evidence. Unsupported newer data, corrupt data, source-fingerprint mismatch, backup conflict, stale revision, path safety failure, and partial publication must retain explicit fail-closed behavior. No point of no return is authorized. The prior approved JAR and exact compatible data snapshot remain rollback material through completion.

### Client, Provider, and Logical-Side Boundaries

Documentation must distinguish server-only mode, enhanced GUI disabled, matching enhanced client, no-SEF fallback client, incompatible enhanced protocol client, mixed clients, reconnect, and server switch. The universal JAR does not make client installation mandatory. The logical server owns permissions, policy, target selection, mutations, sessions, and durable state; clients own presentation only.

Optional providers remain optional and runtime guarded. LuckPerms API 5.4, FTB Essentials, Curios, and every advertised adapter must be described only to the exact present, absent, outage, refresh, removal, malformed, and fallback extent proven in the final matrix. Provider failure never grants authority. A blocked real-provider row blocks the corresponding compatibility claim and release readiness rather than becoming a generic limitation after the fact.

### Operating-System, Opened-Object, and Native Dependency Boundaries

Every user-facing and operator-facing document in the Phase 000 inventory must identify Linux, macOS, and Windows as supported Minecraft Java environments for Java 21, Minecraft 1.21.1, and NeoForge 21.1.235. Platform-specific commands and paths may differ, but support status may not. An unavailable disposable environment is recorded only as an `EXT-001` blocker with the affected rows and required reentry action. It must never become an unsupported, optional, best-effort, or excluded operating-system claim.

Documentation for security-sensitive file publication and append must state that validation and mutation use the same opened object. Linux and macOS documentation must describe the provider-specific or native descriptor boundary and its direct per-operating-system proof. Windows documentation must describe the native handle boundary, reparse state, link count, stable identity through write and flush, and fail-closed behavior. No guide may imply that a path-only precheck, pre-open identity followed by an unrelated write, or silent fallback preserves this invariant. Recovery guidance must explain preservation of prior valid state when identity metadata, native calls, write, or flush fails.

JNA and JNA Platform are compile-only APIs in the mod build and are owned at runtime by the pinned NeoForge 21.1.235 distribution. Documentation must distinguish compile resolution, installed runtime supply, binary compatibility, native loading, and packaged-mod inspection. The candidate mod JAR must contain no duplicate JNA classes, native libraries, implementation, or runtime and must not shadow the platform-owned copy. `EXT-002` separately owns authoritative provenance and alert closure. A platform-owned library, absence from the mod JAR, development-only override, or incomplete upstream advisory does not close or suppress a dependency alert.

### Performance and Reproducibility

Documentation generation and validation must remain bounded and deterministic. It must not start a Gradle or game process per claim, scan outside the repository and approved evidence roots, perform unbounded network calls, or retain entire logs in memory. Parse each tracked document once, index anchors and stable ids once, and compare normalized exact sets. Record generation duration, input count, output count, and peak-size class. Two clean runs with shuffled discovery order and normalized environment paths must produce identical output digests.

Final artifact comparison uses exact bytes, entry inventory, metadata, dependency manifest, source and artifact-input digests, SHA-256, and SHA-512. A matching filename or version is insufficient. Documentation-only changes must not alter `build/libs/sef-2.0.0.jar`; if tool timestamps or nondeterministic packaging prevent byte identity, that is a reproducibility defect and Phase 006 remains open until resolved and rerun.

## Failure, Recovery, and Edge Cases

| Scenario | Detection | Required behavior | Recovery or rollback | Regression proof |
|---|---|---|---|---|
| Phase 006 packet, merge, or signed tag is missing | Packet schema, GitHub state, ancestry, or signature check fails | Stop before editing docs. Do not reconstruct completion from prose. | Complete the missing Phase 006 gate through its owner, then restart Task 001. | Entry-gate fixture and actual tag/packet verification. |
| Phase 006 contains a failed, blocked, stale, or downgraded row | Final ledger or invalidation graph exposes nonpass status or insufficient fidelity | Keep plan incomplete and route to the owning phase. | Obtain the required fixture or repair the defect, integrate, rerun Phase 006, restart Phase 007. | False-completion fixture for each prohibited status. |
| `EXT-001` lacks Linux, macOS, or Windows evidence | External-prerequisite packet or environment exact-set join is incomplete | Stop Phase 007 entry and keep all three operating systems documented as supported. Record the missing environment and affected rows as blocked. | Obtain the disposable environment, rerun the complete required runtime evidence on the frozen candidate, integrate any needed upstream repair, rerun Phase 006, and restart Task 001. | Missing-Linux, missing-macOS, missing-Windows, single-operating-system, and unsupported-platform false-completion fixtures. |
| `EXT-002` provenance or advisory closure is incomplete | Authoritative artifact, resolved runtime, reachability, advisory, remediation, or binary-compatibility gate is unknown or failed | Stop Phase 007 entry. Preserve every platform-owned alert as open or blocked and retain its separate gate results. | Obtain authoritative NeoForge evidence or implement a compatible upstream remedy through Phase 001, then rerun runtime and artifact verification before restarting Task 001. | Platform-owned, compile-only, not-embedded, development-override, and suppressed-alert false-completion fixtures. |
| Opened-object documentation describes a path-only precheck or unrelated write | Terminology and semantic validator cannot join validation and mutation to one descriptor or handle | Fail documentation and security closure. | Correct documentation if product evidence already proves the invariant; otherwise reopen Phase 001 and the affected persistence or integration owner. | Path-only, reopened-path, object-swap, link or reparse, missing-metadata, write-failure, and flush-failure fixtures on Linux, macOS, and Windows. |
| JNA ownership or duplicate-runtime claim is wrong | Build declaration, resolved runtime, native load, JAR entry, or documentation join disagrees | Fail dependency, artifact, documentation, and release-readiness closure. | Correct documentation only when authoritative evidence proves runtime ownership; otherwise reopen Phase 001 and Phase 006 for dependency or packaging remediation. | Compile-only API, missing runtime, binary-incompatible runtime, native-load failure, duplicate class, duplicate native library, and shadowing fixtures. |
| Documentation claim lacks a final evidence id | Claim-ledger foreign-key failure | Mark the claim unsupported; do not infer from implementation presence. | Add valid proof or remove/rephrase the current claim. | Missing-evidence synthetic document and full claim scan. |
| Historical result is phrased as current | Revision or artifact differs from closure identity without historical marker | Fail current-claim gate. | Preserve the original record under explicit historical wording and add current evidence separately. | Wrong-revision fixture and historical/current parser test. |
| Counts match but identities differ | Exact-set symmetric difference despite equal size | Fail reconciliation. | Correct source-owned generator or prose claim through its canonical owner and rerun dependent gates. | Same-count/different-identity fixture. |
| Generated reference was hand edited | Regeneration overwrites it or tracked digest differs from generator output | Reject the edit. | Change the owning registry/generator in its canonical phase or regenerate unchanged output. | Dirty-generated-file test. |
| Unavailable family is advertised as a feature | Capability wording or reference status conflicts with the negative set | Fail release-readiness and docs gates. | Correct documentation; if runtime state changed, reopen Phase 003 and Phase 006. | All sixteen unavailable-as-feature fixtures. |
| Executable capability is described as unavailable | Live action set differs from docs | Fail command/documentation parity. | Correct docs after verifying the live evidence; route a runtime defect upstream if behavior is wrong. | Live-set versus claim-set comparison. |
| Documented command is missing, renamed, or has wrong permission | Operator replay, dispatcher, generated reference, or permission join fails | Record exact mismatch and stop affected closure row. | Correct docs or reopen command/config owner, then rerun replay and Phase 006 as required. | Command/permission replay fixtures. |
| Migration dry run or apply behavior differs from guide | Disposable migration fixture produces different confirmation, path, backup, failure, or rollback result | Do not execute on production and do not soften the guide. | Reopen persistence/integration owner for product defect, or correct guide for already verified behavior. | Forward migration, failed migration, exact recovery, and rollback rehearsal. |
| Rollback cannot restore the pretest snapshot | Hash or semantic state differs after documented recovery | Treat as a persistence integrity defect. | Preserve failure evidence, reopen Phase 002 and Phase 006, repair and rerun. | Before/after hashes and semantic invariant checks. |
| Optional provider fixture is unavailable | Preflight or real login fails before required workflow | Mark row blocked; do not claim compatibility. | Obtain a compatible fixture and rerun, or request an owner plan decision if impossible within pins. | Blocked-provider false-completion test. |
| Link or anchor resolves only on a case-insensitive filesystem | Clean Linux link validator fails exact case | Fail documentation gate. | Correct path or anchor casing and rerun all links. | Case mismatch fixture. |
| Evidence includes a secret, personal datum, host path, or raw production content | Sanitizer or secret/privacy scan matches | Quarantine evidence and invalidate every derived packet. | Owner-led rotation if needed; recreate with synthetic bounded evidence. | Sanitizer corpus and complete rescan. |
| Raw evidence id resolves outside approved root or through unsafe link | Root, normalization, symlink, or hard-link check fails | Refuse the record and preserve safe diagnostic only. | Move or recreate within approved restricted ownership without copying unsafe content. | Traversal, absolute path, symlink, and detectable hard-link fixtures. |
| Product or artifact-input file changes during Phase 007 | Git path classification or input digest differs from Task 001 | Stop closure. Do not include the change in the final docs PR. | Route to earlier owner, integrate sequentially, rerun Phase 006, restart Phase 007. | Product-input drift injection. |
| Rebuilt JAR differs from Phase 006 | Byte hash, entry, metadata, or dependency manifest mismatch | Treat Phase 006 freeze as invalid. | Diagnose reproducibility or upstream change, repair through correct owner, rerun Phase 006. | Two-clean-build byte identity and entry comparison. |
| Documentation-only correction occurs after final audit | Git diff or claim digest changes after Task 010 or Task 012 | Mark dependent final proof stale immediately. | Rerun claim checks, links, operator replay if affected, traceability, final audit, DoD, clean verification, and packet. | Invalidation propagation test. |
| Pull request check, merge, or signature fails | GitHub or local verification reports failure | Do not tag or declare completion; never bypass. | Correct within approved workflow or request owner resolution for unavailable authority. | PR/check/merge/signature state validation. |
| Wiki update would advertise unmerged or unpublished behavior | Wiki content differs from integrated tracked docs or says released | Do not publish the wiki change. | Wait for merge, then copy only integrated release-readiness facts and links. | Post-merge wiki parity and release-wording check. |
| Release broker or platform action is attempted | Command ledger, process audit, or remote state shows preview/upload/publication | Stop and report scope violation. | Preserve artifact unpublished; owner separately authorizes any future release workflow. | No-publication command allowlist and remote release-state comparison. |
| A disallowed defect is found in the final audit | Stable final finding has confirmed evidence | Fail endpoint and assign canonical earlier owner. | Repair in the owning phase, integrate sequentially, rerun Phase 006 and all Phase 007 gates. | Finding-to-owner-to-invalidation test. |

## Verification Matrix

| Requirement or task | Static or unit | Integration | Real workflow or runtime | Negative and recovery | Evidence artifact |
|---|---|---|---|---|---|
| `P007-TASK-001` | Packet, manifest, path-set, ancestry, signature, artifact, `EXT-001`, and `EXT-002` schema validators | Cross-phase packet/tag/commit and separate external-prerequisite join | Clean checkout artifact, Linux, macOS, Windows, and platform dependency evidence preflight | Missing tag, stale packet, wrong ancestry, dirty overlap, wrong artifact, missing operating system, merged or incomplete prerequisite | Phase 007 entry manifest and immutable identity record |
| `P007-TASK-002` | Claim parser, current/historical classifier, support-set, native-boundary, dependency-alert, duplicate-authority, and evidence-id tests | Claim-to-inventory-to-evidence and frozen-identity join | Maintainer review of every exception and ambiguous tense | Unsupported claim, unsupported-platform claim, suppressed alert, stale count, wrong revision, unlabeled history | Final documentation claim ledger |
| `P007-TASK-003` | `ProjectDocumentationGeneratorTest`, `ModuleConfigRegistryTest`, performance generator tests | Live registry-to-reference exact-set reconciliation | Two clean generations at frozen product identity | Manual edit, nondeterministic order, unavailable-as-executable, count-only match | Generated-reference comparison and normalized digests |
| `P007-TASK-004` | Link, anchor, terminology, support-set, opened-object, JNA-ownership, duplicate-runtime, literal-id, version, hash, permission, and compatibility tests | Docs-to-live-contract, docs-to-evidence, and separate-prerequisite joins | Replay Linux, macOS, and Windows setup, diagnostics, client, provider, native-writer, dependency, failure, recovery, migration, and rollback procedures | Unsupported platform, path-only writer, suppressed alert, duplicate runtime, unsafe command, stale path, misleading limitation | Reconciled user/operator docs and cross-platform replay ledger |
| `P007-TASK-005` | Acceptance-status, external-prerequisite, dependency-alert, branch, artifact, release-wording, historical-label tests | Test/audit/acceptance/release exact-set comparison | Review final current acceptance and release-readiness record | Blocked-as-pass, unsupported-as-blocked, alert suppression, history-as-current, stale hash, publication implication | Final acceptance and release documentation record |
| `P007-TASK-006` | Procedure schema, operating-system exact-set, frozen-identity, and expected-result validation | Command-to-permission-to-state-to-evidence and native dependency correlation | Disposable Linux, macOS, and Windows server, client, mixed-client, provider, native-writer, dependency, migration, recovery, rollback, and artifact workflows | Denial, outage, object substitution, reparse or link abuse, missing native API, malformed input, corrupt data, stale revision, incompatible client, rollback failure | Cross-platform operator procedure replay ledger with attempt ids |
| `P007-TASK-007` | Evidence schema, separate-prerequisite, frozen-identity, sanitizer, digest, retention, invalidation, size, and path tests | All phase and external-prerequisite packet/evidence joins | Restricted-root existence and sampled raw-evidence digest verification | Secret, personal data, host path, traversal, symlink, missing raw id, stale proof, merged prerequisite, wrong revision | Final sanitized evidence manifest and raw-evidence index |
| `P007-TASK-008` | Full documentation validation and synthetic mutation suite | `check` lifecycle, generator, support-set, native-boundary, and dependency-alert integration | Clean repository-wide link and claim validation | Broken link, wrong anchor/case, wrong count/hash/version, false capability, unsupported platform, path-only identity, runtime-owner error, duplicate-runtime omission, suppressed alert, stale status | Documentation test reports and injected-failure results |
| `P007-TASK-009` | Stable-id, exact-set, uniqueness, acyclic graph, foreign-key, fidelity, external-prerequisite, and final-identity tests | Requirement-phase-prerequisite-task-test-workflow-artifact-doc join | Human review of exceptions and evidence samples | Orphan, duplicate, circular owner, conflated prerequisite, missing real workflow, wrong artifact or revision | Final plan-wide traceability report |
| `P007-TASK-010` | Matrix schema, same-frozen-identity, and cross-domain invariant checks | Security-dependency-command-UI-persistence-backend-recovery-artifact-documentation-operations-traceability join | Replay selected high-risk Linux, macOS, and Windows end-to-end evidence references at final identity | Contradiction, missing row, disallowed defect, channel mismatch, wrong revision, unsupported platform, suppressed alert, duplicate runtime | Final cross-matrix audit and finding register |
| `P007-TASK-011` | Endpoint and seventeen-clause DoD predicate tests | Invalidation graph, external-prerequisite, and final traceability integration | Review every final predicate against its highest-fidelity evidence | Blocked, stale, mocked, single-operating-system, lower-fidelity, wrong-commit, wrong-artifact, unsupported-platform, path-only writer, duplicate runtime, suppressed alert injection | Definition of Done checklist and false-completion report |
| `P007-TASK-012` | Documentation suite, `check`, generator drift, support-set, JNA ownership, no-duplicate-runtime, secret/path/diff scans | Build, fallback compile, resolved-runtime, artifact-input and byte comparison | Clean rebuild and bounded required diagnostics without repeating lower fidelity for runtime proof | New drift, nondeterministic artifact, runtime provenance mismatch, duplicate runtime, unrelated file, unsafe evidence | Final clean verification ledger, JAR hashes, runtime provenance, and inventory |
| `P007-TASK-013` | Completion-packet schema and diff-scope validation | Independent review findings joined to tasks and evidence | Clean packet replay by reviewer | Unresolved finding, missing exception, protected-file or product change | Reviewed final completion packet |
| `P007-TASK-014` | Commit signature and PR metadata validation | Required checks, review, conversations, merge method and target | GitHub pull-request merge | Failed/pending check, wrong target, bypass, direct push, wrong merge method | PR, checks, merge commit, signature records |
| `P007-TASK-015` | Tag signature, packet digest, support-claim, external-prerequisite, wiki-link, and release-wording validation | Post-merge candidate, repository tracking, and wiki synchronization | Clean checkout checks and artifact comparison at merge commit | Tag on wrong commit, stale packet, unsupported-platform wiki claim, suppressed alert, wiki advertises release, remote mismatch | Signed final tag and read-back synchronization record |
| `P007-TASK-016` | Exact endpoint predicate evaluator | All requirements, phases, external prerequisites, DoD, artifact, integration and no-publication joins | Owner-facing final completion record | Any false or unknown predicate, including either external prerequisite, produces incomplete, never partial success | Final plan-completion record |

### Fixtures, Environments, Expected Results, and Rerun Order

- Use Java 21 and the checked-in Gradle wrapper from clean disposable checkouts of the exact frozen candidate revision on Linux, macOS, and Windows. Record operating-system version, architecture, filesystem, Java vendor and version, wrapper version and digest, Minecraft 1.21.1, NeoForge 21.1.235, Parchment 2024.11.17, mod id, product version, commit, signed tag, artifact hashes, resolved runtime dependency identities, and fixture ids. Do not record environment-variable values.
- Require exact-set equality for the three mandatory operating systems. A missing environment leaves `EXT-001` and every dependent row blocked while the target remains supported. Separately validate `EXT-002` authoritative NeoForge provenance, runtime supply, compile-only JNA compatibility and native loading, no duplicate runtime, and every candidate-graph, packaged-mod, installed-runtime, reachability, advisory, and compatible-remediation result.
- Reuse the Phase 006 disposable server, enhanced-client, fallback-client, incompatible-client, mixed-client, provider, migration, recovery, performance, and artifact fixture definitions only when their digests match. Never reuse a world or provider state whose revision is unknown.
- Use synthetic actors, synthetic messages, synthetic addresses, disposable worlds, loopback networking, task-owned configuration roots, and nonproduction provider fixtures. No test may point at a production server, world, account, credential store, or user data.
- Expected success means the documented state and output match the final artifact exactly. Expected failure means the command is denied or fails safely with the documented bounded feedback, no unauthorized mutation, correct audit result, retained recovery evidence where applicable, and no sensitive disclosure.
- Generated output is valid only when two clean runs match byte-for-byte after approved normalization and the tracked tree shows zero unexplained drift. Count equality without identity equality fails.
- Execute final proof after the last Phase 007 change in this order:
  1. Validate protected plan and goal integrity, candidate ancestry, phase packets, signed tags, worktree classification, separate complete `EXT-001` and `EXT-002` records, and the frozen product and artifact identities shared by every final evidence domain.
  2. Run `./gradlew check` and every maintained documentation, link, claim, static-analysis, unit, coverage, and drift gate registered by earlier phases. Run focused documentation, evidence-schema, sanitizer, traceability, and generator tests when the aggregate failure requires bounded diagnosis.
  3. Run `./gradlew generateProjectReferences generatePerformanceReport`, capture normalized digests, run it again from a clean state, and require identical output and zero unexplained tracked drift.
  4. Rerun `./gradlew check` after generation so the final tracked state passes every maintained quality and drift gate.
  5. Run the documented operator procedure replay on Linux, macOS, and Windows, including native opened-object, runtime dependency loading, failure, recovery, and rollback procedures. Do not rerun destructive cases outside disposable fixtures. Do not substitute a documentation replay for the complete Phase 006 runtime matrix.
  6. Run `./gradlew build compileFallbackRuntimeJava` from the clean checkout, inspect `build/libs/sef-2.0.0.jar`, calculate SHA-256 and SHA-512, compare entries, metadata, dependencies, and artifact inputs with Phase 006, prove the pinned NeoForge runtime supplies the compile-only JNA APIs, and require no duplicate JNA class, native library, implementation, or runtime.
  7. Validate the sanitized evidence manifest, raw-evidence digests and routing, final traceability, final cross-matrix audit, separate external-prerequisite closure, false-completion tests, and all seventeen Definition of Done clauses. Require security, dependency, command, UI, persistence, backend, recovery, artifact, documentation, operations, and traceability evidence to share the same frozen product revision and artifact digest.
  8. Scan tracked documentation, bounded summaries, the final JAR, and complete Git diff for secrets, personal data, private host paths, credentials, logs, caches, worlds, generated junk, debug output, unrelated changes, and platform/version drift.
  9. Assemble and validate the packet, preflight private independent-review capability, complete one review when supported or record the optional unsupported disposition, and rerun every gate invalidated by review corrections.
  10. After pull-request merge, repeat plan/tag/packet integrity, documentation/reference drift, artifact identity, endpoint, and no-publication checks from a clean checkout before creating the signed final phase tag.
- `./gradlew runGameTestServer`, dedicated server, client, multiplayer, provider, UI, recovery, and performance proof remains required through the Phase 006 packet. Phase 007 reruns a row only when its invalidation set intersects a Phase 007 change or the operator procedure itself must be demonstrated. If product or harness changes require such a rerun, Phase 006 reopens and must run its complete prescribed matrix.
- A failed or blocked mandatory result stops completion. No timeout, unavailable Linux, macOS, or Windows environment, unavailable graphical environment, unavailable provider, missing authoritative dependency evidence, remote API limitation, or review unavailability may be called a pass. Operating-system unavailability remains `EXT-001`; authoritative platform dependency unavailability remains `EXT-002`. Optional independent review unavailability is recorded as unavailable only when repository rules classify it as optional; deterministic gates remain mandatory.

## Documentation, Operations, and Release

### Required Documentation Reconciliation

1. Update `README.md` with Linux, macOS, and Windows as supported Minecraft Java environments, the final pinned platform and runtime ownership, current release-readiness status, verified feature summary, intentionally unavailable capability boundary, exact installation and development fast paths for each operating system, configuration entry points, compatibility and limitations, support paths, and links. State that missing environment evidence is an `EXT-001` blocker rather than unsupported-platform status. Remove or supersede stale counts, branches, hashes, and open-row claims.
2. Update `DOCUMENTATION.md` with final architecture facts, initialization, registration, permissions, configuration, persistence, networking, client/server boundaries, integrations, verification commands and expected results for Linux, macOS, and Windows, failure handling, recovery, troubleshooting, security/privacy, release preparation, and completion status. Describe provider-specific or native same-opened-object validation and mutation, fail-closed recovery, compile-only JNA and JNA Platform ownership by pinned NeoForge, no duplicate native runtime, and separate `EXT-001` and `EXT-002` evidence. If maintained format, static-analysis, warning, or coverage gates were added earlier, remove the old statement that they do not exist and document the exact final commands.
3. Regenerate `docs/COMMAND_REFERENCE.md`, `docs/PERMISSION_REFERENCE.md`, and `docs/CONFIGURATION_REFERENCE.md` only with `./gradlew generateProjectReferences`. Regenerate `docs/PERFORMANCE_REPORT.md` only with `./gradlew generatePerformanceReport`. Record exact final input and output digests and require zero drift.
4. Reconcile `docs/INSTALLATION.md` for Linux, macOS, and Windows server-only, enhanced, fallback, incompatible, and mixed-client setup; pinned NeoForge runtime ownership; optional providers; first start; permissions; diagnostics; updates; and safe failure behavior.
5. Reconcile `docs/COMPATIBILITY_MATRIX.md` with the exact Phase 006 Linux, macOS, and Windows platform, client, registry/content, storage, protocol, native-writer, dependency-loading, and provider evidence. A missing current real-provider workflow is a blocker, not a compatibility claim. A missing operating-system fixture is `EXT-001`, not an unsupported target.
6. Reconcile `docs/MIGRATION_GUIDE.md` and recovery sections with every actual schema or configuration migration, source fingerprint, backup, validation, opened-object failure, write or flush failure, restoration, retry, rollback, prior-JAR, and no-point-of-no-return result on every platform-sensitive path. If no final product change introduced migration, say so precisely while preserving the verified existing migration instructions.
7. Reconcile `docs/SECURITY_REVIEW.md` with final trust boundaries, provider-specific or native opened-object proof on Linux, macOS, and Windows, compile-only JNA ownership and native loading, no duplicate runtime, separate candidate and platform dependency dispositions, adversarial proof, limitations, invalidation, artifact scan, and the exact endpoint defect boundary. Never claim mathematical absence of defects and never suppress an alert because it is platform owned or not embedded in the mod JAR.
8. Reconcile `test.md`, `audit.md`, the Phase test records, and `docs/SEF2_ACCEPTANCE.md`. Preserve original dated results and commits as history. Add one final current ledger tied to the same frozen product commit, closure commit, artifact hashes, Linux, macOS, and Windows environments, distinct `EXT-001` and `EXT-002` results, passed mandatory rows, and any explicitly excluded future work.
9. Reconcile `docs/RELEASE_NOTES.md` and `docs/RELEASE_NOTES_2.0.0.md` with only verified behavior. Do not list unavailable families as additions. State release readiness, not public availability, until separately published.
10. Reconcile `docs/RELEASE_WORKFLOW.md` with the actual approved candidate ref, exact artifact and checksum outputs, Linux, macOS, and Windows evidence requirements, compile-only JNA runtime provenance, no-duplicate-runtime inspection, separate `EXT-001` and `EXT-002` gates, required checks, no-publication boundary, rollback, and separate owner authorization for any future preview or publish action. Do not invoke the broker or verify marketplace state in this phase.
11. Update every other user-facing or operator-facing document in the Phase 000 documentation inventory so it explicitly identifies Linux, macOS, and Windows as supported Minecraft Java environments and accurately carries opened-object, native dependency, external-prerequisite, present-tense claim, command, permission, path, config, compatibility, security, migration, recovery, troubleshooting, test, acceptance, performance, installation, or release facts. Do not create duplicate authorities or generic placeholder documents.

### Required Operator Information

Each applicable user or operator workflow must name:

- Linux, macOS, and Windows as supported Minecraft Java environments, plus supported Minecraft, NeoForge, Java, loader, mapping, wrapper, mod, protocol, client, and integration versions. A missing environment must be labeled as the relevant `EXT-001` blocker, never unsupported.
- Server-only, enhanced-client, fallback-client, incompatible-client, and mixed-client setup and observable behavior.
- Exact command and permission literals, source and target limits, dangerous denied-by-default capabilities, wildcard risks, revision behavior, and current diagnostics.
- Configuration file ownership, reload versus restart behavior, typed validation, secret filtering, prior-known-good state, and generated-reference command.
- Invalid input, authorization denial, provider absence/outage, registry failure, persistence recovery, partial failure, incompatible version, and shutdown failure behavior.
- Provider-specific or native opened-object identity behavior for security-sensitive writes, including same descriptor or handle validation and mutation, link or reparse rejection, stable identity through flush, fail-closed handling, and recovery of prior valid state.
- Compile-only JNA and JNA Platform ownership by pinned NeoForge 21.1.235, exact runtime supply and native loading, prohibition on duplicate JNA classes or native libraries in the mod JAR, and the separate `EXT-002` provenance and alert-closure procedure.
- Backup prerequisites, disposable rehearsal requirements, recovery paths, migration dry run, confirmation binding, failure restoration, rollback procedure, prior approved JAR and data snapshot, and verification before reopening service.
- Optional provider present, absent, failed, refreshed, removed, and fallback behavior. Never describe provider failure as permissive.
- Known limitations and explicit exclusions, including all sixteen unavailable families and the fact that publication and deployment are not authorized by release readiness.
- Exact evidence commands, expected outputs, timeouts, log or report location class, sanitization behavior, failure interpretation, and which bulky raw outputs stay outside tracked source.
- Separate `EXT-001` and `EXT-002` status, evidence ids, affected rows, blocker interpretation, and reentry action. Operator text must not conflate the prerequisites, narrow operating-system support, or suppress a dependency alert.

### Repository Integration and Remote State

- Phase 007 changes remain on its sequential `envy` phase branch until all local gates and review pass.
- The final phase pull request is the only integration path. Required checks and conversations must complete. Private independent-review capability must be preflighted, with one supported review completed or the explicit optional unsupported disposition recorded. Use the repository-required merge-commit method and never bypass protection or push the result directly.
- After GitHub reports the pull request merged, verify the approved candidate or default integration target contains the merge and remains on the selected SEF 2 lineage. From a clean checkout, rerun the final nonmutating closure checks before tagging.
- Create and push one signed annotated final phase tag on the merged closure commit. Verify the signature and remote target. Do not delete historical phase branches or tags.
- Only after approved integration and tag verification, synchronize the Phase 007 issue, pull request, Project fields, final milestone, release-readiness state, and applicable closed issues. Close an item only when its acceptance criteria are integrated and verified.
- Only after approved integration, update the GitHub wiki from tracked canonical documentation. The wiki may link to the final README, technical docs, installation, compatibility, migration, security, acceptance, release notes, release workflow, issues, Project, milestones, and releases. It must say release ready, not released, unless a separate later publication has actually occurred.
- Search remote objects before updating them, preserve established ids, and read back every changed remote state. Remote synchronization must not modify the saved goal or protected plan files.

### Release Boundary and Rollback

This phase records release readiness only. It creates no release upload, GitHub release, platform draft, marketplace preview, deployment, credential, production change, or public publication. A later release task must obtain the owner's explicit authority, freshly verify platform mappings and artifact hashes, and use the then-current release workflow.

Before Phase 007 merge, rollback means removing only Phase 007-owned documentation, evidence-summary, and validator changes from its branch. After merge, rollback uses the signed Phase 006 integrated state for the previous product-and-doc baseline and the Phase 007 merge commit for the closure comparison. Product data is not migrated in this phase. The prior approved JAR and exact compatible data snapshot remain retained as documented rollback material. A Phase 007 rollback cannot make an unsafe or stale earlier artifact release ready; it merely restores the prior documented state.

## Risks and Evidence Invalidation

| Risk | Prevention | Detection | Recovery | Evidence invalidated | Reverification |
|---|---|---|---|---|---|
| Current prose carries historical counts or hashes | Claim ledger binds every numeric/artifact claim to revision and evidence id | Current/historical classifier and artifact comparison | Label historical or replace with final verified value | Affected claim, links, acceptance, release notes, packet | Claim suite, doc links, traceability, final audit |
| Documentation advertises schema presence as runtime capability | Separate schema, executable, fallback, and unavailable states | Live action/handler exact-set join and negative-family word scan | Correct docs; reopen Phase 003 if runtime is wrong | Command, UI, compatibility, release claims | Generated refs, Phase 003 rows, docs and DoD |
| Generated references drift after manual edits | Generator ownership and exact tests | Clean regeneration changes tracked file | Remove manual edit and regenerate | Generated reference and dependent docs | Two clean generations, focused tests, links |
| Final docs change product or artifact inputs | Strict path classification and artifact-input digest | Git diff or rebuilt artifact differs | Route change upstream, rerun Phase 006, restart Phase 007 | Entire Phase 006 and Phase 007 evidence | Full upstream owner gates, Phase 006 matrix, all Phase 007 tasks |
| A required real workflow is replaced with static proof | Fidelity field and canonical row type | Traceability shows absent required runtime evidence | Obtain fixture and run workflow | Affected requirement, phase, DoD, endpoint | Highest-fidelity workflow and all downstream joins |
| Evidence from another commit or artifact is reused | Bind every row to product commit and artifact hash | Final identity mismatch | Mark stale and rerun at final identity | Row and all dependent claims/matrices | Required row, traceability, final audit and DoD |
| A user or operator document calls Linux, macOS, or Windows unsupported | Exact support-set validator over the complete Phase 000 documentation inventory | Document claim set differs from `DEC-009` or treats missing `EXT-001` evidence as exclusion | Correct the claim, preserve the target as supported, record the missing evidence as blocked, and rerun every affected documentation and endpoint gate | Support claims, compatibility, installation, acceptance, release readiness, traceability, DoD, packet | Full documentation scan, `EXT-001` join, cross-platform replay, final audit |
| `EXT-001` and `EXT-002` are conflated | Stable prerequisite ids and independent result and evidence fields | One record, one result, or one evidence packet is used for both contracts | Split the records, restore each missing gate, and keep completion blocked until both pass independently | External-prerequisite closure, affected requirements, traceability, DoD, endpoint | Separate schema and foreign-key validation, prerequisite false-completion tests |
| Platform-owned alert is suppressed or treated as inapplicable solely because JNA is compile only or not embedded | Separate graph, packaged-mod, installed-runtime, reachability, advisory, and compatible-remediation gates | Any gate is omitted, dismissed, or inferred from ownership or packaging | Restore the alert and every separate disposition, obtain authoritative `EXT-002` evidence or repair through Phase 001, then rerun Phase 006 and Phase 007 | Dependency, security, artifact, compatibility, release-readiness, DoD, endpoint | Authoritative provenance, runtime graph, reachability, advisory, remediation, artifact inspection |
| Documentation weakens opened-object identity to a path check | Same-object terminology and evidence-id join | Validation and mutation do not resolve to one opened descriptor or handle through flush | Correct docs only if final evidence proves the invariant; otherwise reopen the security, persistence, or backend owner and rerun downstream phases | Security, persistence, backend, recovery, documentation, traceability, DoD | Linux, macOS, and Windows native writer and failure matrix at frozen identity |
| Duplicate JNA implementation or native runtime enters the artifact | Compile-only declaration, artifact entry inventory, dependency manifest, and runtime provenance comparison | JAR or nested artifact contains JNA classes or native libraries, or shadows pinned runtime supply | Reopen Phase 001 and Phase 006, repair packaging, and restart Phase 007 after a new frozen handoff | Dependency, security, artifact, runtime, documentation, DoD, endpoint | Clean build, JAR inventory, runtime load on all three operating systems, `EXT-002` closure |
| Evidence leaks private or production data | Synthetic fixtures, sanitization, restricted raw root | Secret/privacy/path scan | Quarantine, rotate if needed, recreate | Unsafe row and every packet containing it | Sanitizer, raw digest, full manifest and packet scans |
| Documentation links work locally but fail remotely | Exact case-sensitive relative-link and anchor validation | Clean Linux validator and wiki read-back | Correct canonical path/anchor | Link graph, navigation, wiki parity | Full link suite and post-merge read-back |
| Migration or rollback guide is not executable | Disposable end-to-end rehearsal with hashes | Step, command, path, or state differs | Correct docs or reopen persistence owner | Migration, recovery, compatibility, release readiness | Rehearsal, Phase 002/006 as applicable, docs and DoD |
| Optional provider compatibility is inferred from mocks | Fidelity and provider provenance requirements | Missing real compatible provider environment | Keep blocked until real fixture runs | Provider, compatibility, integration, release readiness | Real provider matrix and all dependent Phase 006/007 gates |
| Artifact build is nondeterministic | Two clean builds and exact byte/entry comparison | Hash mismatch without input drift | Repair packaging reproducibility upstream | Artifact and every hash-bound row | Full Phase 006 artifact matrix and Phase 007 closure |
| Final domain matrices use different product revisions or artifacts | Common frozen-identity foreign keys across every final record | Security, dependency, command, UI, persistence, backend, recovery, artifact, documentation, operations, or traceability row has a different commit or digest | Mark the mismatched row and all dependents stale, rerun at the frozen identity, and restart the downstream joins | Cross-matrix audit, traceability, DoD, packet, endpoint | Common-identity exact-set validator and complete final cross-matrix audit |
| Last-minute prose edit bypasses final validation | Freeze after Task 012 and diff digest in packet | Commit tree differs from verified tree | Rerun invalidated documentation and packet gates | Claim, link, traceability, audit, DoD, packet | Tasks 008 through 013 as determined by invalidation graph |
| Remote state implies release or closes work too early | Update only after merge/tag and use release-readiness vocabulary | Remote read-back differs from packet | Reopen affected item and correct state | Remote synchronization and endpoint record | GitHub/wiki read-back and final packet comparison |
| Final tag points to the wrong commit | Create only after post-merge verification | Tag target or signature mismatch | Do not declare completion; create correct signed tag without rewriting an existing published tag unless owner authorizes recovery | Integration, packet, endpoint | Commit ancestry, tag signature and remote target verification |
| A late defect is documented as a limitation | `DEC-003` defect classes are nonwaivable | Final finding classification intersects disallowed endpoint | Reopen canonical phase and remediate | Owning phase plus all downstream evidence | Regression proof, Phase 006, full Phase 007 closure |
| Release preparation accidentally invokes publication | Explicit command allowlist and no credentials | Process/remote audit sees broker preview, upload, or release object | Stop and report; preserve artifact unpublished | Release-boundary and remote-state proof | No-publication check and fresh remote read-only snapshot |
| Protected plan or saved goal is edited as status | Protected-path diff gate | Git diff or digest mismatch | Restore only through owner-authorized protected workflow; Phase 007 stops | Plan integrity and all closure evidence | Plan/index/goal digest verification and restart Task 001 |

Any change to product source, resources, build metadata, wrapper, dependencies, schemas, protocol, command catalog, permissions, configuration registry, persistence, lifecycle, UI, integration, tests that establish behavior, runtime harness, generator semantics, artifact packaging, or artifact inputs invalidates the Phase 006 freeze and the complete Phase 007 closure. A documentation-only change invalidates every claim, link, replay, traceability, audit, DoD, and packet row that names it. A remote-only tracking correction after the tag invalidates remote synchronization evidence but not local runtime proof unless it changes a release or branch identity. The final packet records each invalidation decision; silence never means unaffected.

## Phase Completion Packet

The final Goal Creator completion packet required by `SEFAUD-REQ-009` is stored outside the protected plan set using the Phase 000 evidence contract. Producing it does not invoke Goal Creator or edit, refresh, or rebind the immutable saved goal. It must contain all of the following before the plan may close:

1. Master-plan, plan-index, registered-phase, and saved-goal integrity results, including the active final phase and unchanged owner decisions.
2. Contiguous Phase 000 through Phase 007 ancestry; phase commit lists; pull request ids; required checks; review dispositions; merge commits; signed annotated phase tags; tag targets; and signature verification.
3. The Phase 006 verified product commit, Phase 007 closure commit, selected candidate/integration ref, source-tree digest, artifact-input digest, dependency-manifest digest, JAR path, byte size, entry-inventory digest, SHA-256, SHA-512, and proof of unchanged artifact identity.
4. Java, Gradle wrapper, Minecraft, NeoForge, Parchment, mod id, product version, and complete Linux, macOS, and Windows operating-system version, architecture, filesystem, client/provider fixture, resolved-runtime, and evidence-environment manifests without secret values or private paths.
5. Final tracked documentation path inventory, link graph, claim ledger, current/historical classifications, evidence bindings, and exact correction list. Include an exact support-claim result proving every user and operator document identifies Linux, macOS, and Windows as supported Minecraft Java environments and no blocked environment is called unsupported.
6. Two clean generated-reference digests and zero-drift comparisons for command, permission, configuration, and performance references, plus live executable and unavailable exact-set reconciliation.
7. Documentation, link, anchor, terminology, claim, version, count, capability, three-operating-system support, provider-specific or native opened-object, compile-only JNA ownership, no-duplicate-runtime, distinct external-prerequisite, platform-alert, generated-drift, secret, privacy, path, and artifact-hash test results, including synthetic false-completion cases.
8. The Linux, macOS, and Windows operator procedure replay ledger for installation, update, permissions, diagnostics, configuration, failures, opened-object audit writing, runtime dependency loading, recovery, migration, rollback, enhanced/fallback/incompatible/mixed clients, optional providers, artifact inspection, and evidence generation, with expected and actual results and attempt ids.
9. The final sanitized evidence manifest and approved raw-evidence index. Each raw item is linked by stable relative evidence id and digest, never by private absolute path. Include sanitizer, retention, access, invalidation, same-frozen-identity, and distinct-prerequisite results.
10. Final requirement-to-phase-to-external-prerequisite-to-task-to-inventory-row-to-test-to-runtime-workflow-to-artifact-to-documentation traceability for all nine requirements, all eight phases, `EXT-001`, and `EXT-002`, with exact-set, foreign-key, uniqueness, acyclic, fidelity, common frozen-identity, and separate-prerequisite validation.
11. Final security/privacy, candidate and platform dependency, administrator command, graphical and textual UI, persistence, backend integration, recovery and rollback, compatibility, performance, artifact, documentation, operations, traceability, and release-readiness cross-matrix audit with zero unresolved mandatory finding and proof that every domain names the same frozen Phase 006 product revision and artifact digest.
12. False-completion and stale-evidence report covering failed, blocked, incomplete, mocked, single-operating-system, lower-fidelity, wrong-revision, wrong-artifact, missing-fixture, missing-row, stale-after-change, unsupported-platform, path-only-writer, compile-only-without-runtime, duplicate-native-runtime, suppressed-platform-alert, conflated-prerequisite, and unavailable-as-feature cases.
13. A clause-by-clause result for all seventeen master Definition of Done conditions and every `SEFAUD-REQ-009` acceptance criterion and evidence item.
14. Final clean-checkout command ledger in prescribed order, generated drift result, `check`, build, fallback compilation, Linux, macOS, and Windows procedure evidence joins, resolved runtime and native loading validation, artifact comparison, no-duplicate-runtime result, secret/privacy/path scan, and complete diff audit.
15. Final reconciled `README.md`, `DOCUMENTATION.md`, generated references, compatibility, installation, migration, recovery, security, troubleshooting, test, acceptance, performance, release notes, release workflow, and every other user or operator documentation file digest, plus the exact three-operating-system support-set result.
16. Preservation proof for unrelated user state and confirmation that no protected plan/goal, product behavior, artifact input, platform version, credential, production data, or unrelated file entered Phase 007.
17. Rollback statement naming the signed Phase 006 state, Phase 007 merge commit, retained prior approved JAR and data snapshot, and confirmation that no schema migration, point of no return, production mutation, deployment, preview, upload, release, or publication occurred.
18. Final repository synchronization record for issue, pull request, Project, milestone, and wiki state after integration, with remote read-back and release-readiness wording validation.
19. Signed final phase tag, post-merge clean verification, packet digest, final release-readiness record, and no-publication proof.
20. The exact owner-selected endpoint, each deterministic predicate result, separate `EXT-001` and `EXT-002` results, and the unqualified final result. If any predicate is false or unknown, the packet names the canonical owner, external blocker where applicable, invalidated evidence, and required reentry task instead of claiming completion, narrowing operating-system support, or suppressing a dependency alert.

Raw logs, recordings, screenshots, profiles, dependency reports, corrupted fixtures, migration snapshots, recovery snapshots, world data, and full runtime traces remain in the approved restricted evidence root. The packet contains only bounded sanitized summaries and relative evidence ids with digests. The packet is not written into this phase file or any protected planning artifact.

## Next Transition

There is no later implementation phase. After the Phase 007 pull request is merged, the integrated closure revision must be checked out cleanly and revalidated for protected-plan integrity, phase ancestry, separate `EXT-001` and `EXT-002` closure, Linux, macOS, and Windows support wording, opened-object and compile-only JNA claims, documentation and generated-reference drift, evidence and traceability integrity, artifact byte identity and no duplicate runtime, Definition of Done, release-readiness wording, and no-publication state. Only after those checks pass may the signed annotated final phase tag be created and pushed. Repository tracking and wiki synchronization then reflect the integrated, tagged release-readiness state.

`P007-TASK-016` evaluates this exact owner-selected endpoint:

> At one frozen SEF 2 candidate revision and artifact digest, every mandatory audit and remediation matrix is complete; all confirmed in-scope defects are repaired; highest-fidelity clean-checkout, dedicated-server, matching-client, native-writer, dependency, shutdown, restart, and representative administrator workflow verification passes on Linux, macOS, and Windows; `EXT-001` and `EXT-002` are satisfied; documentation and evidence identify all three operating systems as supported and match the built artifact; the mod embeds no duplicate native runtime; every platform-owned dependency alert passes its separate closure gates; and no known critical or high exploitable vulnerability, authorization bypass, sensitive-data leak, executable administrator-command defect, UI-blocking defect, persistence-integrity defect, or mandatory backend-integration defect remains.

The endpoint passes only if every mandatory requirement, every phase exit and integration gate, `EXT-001`, `EXT-002`, every Section 18 Definition of Done clause, every final traceability row, the common frozen product and release artifact identity, the signed final tag, and the no-publication boundary pass simultaneously. `FUT-001`, `FUT-002`, and `FUT-003` remain excluded. Release readiness does not authorize publication.

If any predicate is failed, blocked, stale, incomplete, unavailable at required fidelity, tied to another revision or artifact, or invalidated by a later change, the plan remains incomplete. The completion record must name the exact failing stable ids, including `EXT-001` and `EXT-002` separately where applicable, canonical owner, evidence that was invalidated, and the first required reentry task. It must not emit partial, conditional, or qualified success, narrow operating-system support, or suppress a dependency alert.
