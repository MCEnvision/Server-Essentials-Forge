# phase 000 fixture register

This register is created before any fixture process or candidate file. It binds the task owned resources and teardown scope for `P000-TASK-002`.

## ownership

| item | value |
| --- | --- |
| host | `node-1` |
| task | `P000-TASK-002` |
| node disposable root | `/mnt/hermes/projects/Sef/.test-runs/phase-000/fixture-20260925-e` |
| laptop disposable root | `/home/envy/.local/share/PrismLauncher/instances/sef-phase-000-client-20260925-e` |
| retained evidence | `/mnt/hermes/projects/Sef/docs/verification/phase-000/` |
| proxy process | only a Velocity process started from this register's candidate file |
| backend processes | only Forge server processes started from this register's disposable runtimes |
| client process | only a Forge client started from the registered laptop instance path |
| audio watcher | only a watcher bound to the exact owned client process tree and stream |

## planned resources

The disposable roots may contain downloaded candidate jars, proxy configuration, two backend runtimes, logs, worlds, eula files, generated server properties, an isolated Prism client instance, client logs, and sanitized intermediate receipts. No production path, the existing FutureShops instance, credential, shared database, or sibling checkout is in scope.

Before startup, every created path is checked against this register. Before cleanup, every owned process, watcher, stream, and child path is resolved by exact identity. After the final evidence consumer, owned processes stop and the disposable root is removed. Required sanitized receipts remain under the retained evidence directory. Any residue remains an open phase gate and is not silently deleted.

## launch boundary

The fixture uses pinned external Velocity, Ambassador, and ProxyCompatibleForge artifacts only. It does not create the SEF bridge, shared authority, administrative plugin channel, custom handshake, production topology, or product implementation. The Forge target is tested as a no GUI server fixture. A laptop client is required for signed chat, real player command arguments, and both directions of backend switching. If the exact laptop window, discrete renderer, private route, and muted owned playback stream cannot be proven, those assertions remain unverified.

The `fixture-20260925-e` retry uses the pinned Forge 47.3.12 server installer, the reobfuscated target jar, MixinExtras Forge 0.5.3 on both sides, PCF `MODERN_DEFAULT` forwarding override on both backends, a private Tailscale proxy listener, and a separate Forge client instance. It does not alter the target source, disable a mixin, substitute a Forge version, or touch the existing FutureShops instance.

## follow up retry extension

The later `fixture-20260925-g` retry used the same bounded ownership rules with a freshly built SEF jar. Its node disposable root was `/mnt/hermes/projects/Sef/.test-runs/phase-000/fixture-20260925-g`, and its laptop disposable root was `/home/envy/.local/share/PrismLauncher/instances/sef-phase-000-client-20250925-g`. The extension owned only the proxy, the two Forge processes, the isolated client process, and their children. The pre-existing Docker listeners on `192.168.1.242:25566` and `192.168.1.242:25567` were identified and preserved as out of scope.

The client sound engine failed during startup and no owned playback stream was created. Per the host acceptance boundary, the client was stopped without attempting further interaction. The extension therefore does not claim a backend switch or a reconnect result.

## x11 client retry extension

The exact owned paths were registered before launch as `/mnt/hermes/projects/Sef/.test-runs/phase-000/fixture-20260925-h` and `/home/envy/.local/share/PrismLauncher/instances/sef-phase-000-client-20250925-h`. The client launched through the laptop's existing X11 display with master output zero before startup. Its exact window, NVIDIA renderer, process tree, and muted playback stream were verified before test input. A command-only follow up used the same registered paths. The node root, client instance, proxy, Forge backends, logs, worlds, candidate downloads, screenshot, launch log, and any audio watcher are disposable; only sanitized evidence under this directory is retained.

The exact laptop scratch files found during teardown were `/tmp/sef-phase-000-client-20250925-h-command.png` and `/tmp/sef-phase-000-client-20250925-h-launch2.log`. They are included in the h cleanup scope. No other matching h scratch file was found.

## reset capable client retry

The `fixture-20260925-i` retry owns only `/mnt/hermes/projects/Sef/forge-1.20.1/1.1/.test-runs/phase-000/fixture-20260925-i`, the initially absent checkout `build` output created for its final server artifact, and `/home/envy/.local/share/PrismLauncher/instances/sef-phase-000-client-20260925-i` on the verified `envision` laptop. The node and laptop roots were checked absent before allocation. Pre-existing checkout `.gradle` state, personal instances, other task worktrees, and unrelated processes are protected. The exact proxy, backend, client, launcher, and audio watcher PIDs and owned stream IDs must be recorded after launch, then checked absent after the final evidence consumer. No other directory is a cleanup target.

The source branch is `envy/sef-phase-000` at `e59d90c95375936349222ef7667510d029c628e4`. This retry adds the owner-linked Forge Client Reset Packet Forward file `4657349` only to the isolated client. It retains the pinned Velocity, Ambassador, PCF, Forge and MixinExtras candidates. Candidate hashes, notices, advisory state, Java and loader identity, exact generated configuration, current private listener, inspected headless task graph, `eula=true` readback, laptop desktop and discrete renderer must be captured before runtime claims. Client master output must be zero before launch; an owned playback stream must be correlated and read back muted before input, then monitored through any switch or stream recreation. The run stops safely if that proof fails. Only sanitized decisive evidence under this verification directory survives teardown.

## fixture 20260925 k, registered before creation

This retry owns `/mnt/hermes/projects/Sef/forge-1.20.1/1.1/.test-runs/phase-000/fixture-20260925-k`, the initially absent checkout `build` output if the production artifact must be rebuilt, and `/home/envy/.local/share/PrismLauncher/instances/sef-phase-000-client-20260925-k` on the verified `envision` laptop. These exact paths were checked absent before registration. Existing `.gradle` caches, personal instances, the active SkyBlock Enhanced client, unrelated processes, listeners, worktrees, and databases are excluded and must remain untouched.

Only candidate downloads, generated Velocity configuration, two disposable Forge server runtimes, EULA files, logs, worlds, test-only secrets, a separately created Forge client instance, and the bounded audio watcher and its log may exist below the registered roots. The production target JAR is built from the registered checkout and copied into this fixture; the `build` output is removed only after its final consumer. The proxy binds only the current Tailscale interface, the backends bind loopback, and ports are rechecked before allocation. No public listener, firewall change, external production endpoint, credential, personal instance, or existing server listener is in scope.

The intended candidates remain Forge 47.3.12, Velocity 4.2.0 build 30, Ambassador 1.4.5, ProxyCompatibleForge 1.3.1, MixinExtras Forge 0.5.3, and the owner-selected reset mod file 4657349. The reset mod is client-only. Exact bytes, notices, advisory observations, generated configuration, profile contents, hosts, JDKs, process IDs, renderer, window, stream IDs, and bounded scenario receipts must be recorded before they support a claim. Each backend requires a fresh `eula=true` readback before launch.

The laptop acceptance client is held until the unrelated user-owned desktop session is no longer active or the owner explicitly directs otherwise. If launched, its isolated master volume is set to zero before startup; only its exact process tree and PipeWire stream may be muted or watched. Confirm actual window, PID, discrete NVIDIA renderer, private proxy route, initial stream mute readback, client reset capability, and the same-session switch state before counting any interaction. Do not touch the user's currently open client or use it as fixture evidence.

After the final evidence consumer, save both backend worlds and stop the exact Forge processes, interrupt the exact proxy, stop the exact client and audio watcher, verify the owned stream and process exits, then remove only the registered `k` roots and registered build output. Preserve only sanitized evidence in this phase directory. Any unverified client gate or residue remains explicit and open.

## k preflight observations

At 2026-09-25 21:29 UTC, the four pinned Velocity, Ambassador, ProxyCompatibleForge, and client reset artifacts were fetched into the registered `k/candidates` directory and their SHA-256 and SHA-512 values matched the recorded candidate identities. The candidate JARs were inspected but not executed. The reset JAR reports mod ID `clientresetpacket` and internal version `0.0.6`; its external file is 4657349 and its filename says 0.3.0. The separate artifact integrity record contains the complete hashes, notice findings, license references, and advisory observation.

At 2026-09-25 21:33 UTC, the Forge task graph check ran with Java 17.0.20.1 using the existing wrapper through Bash. `bash ./gradlew --offline --no-daemon runServer --dry-run --console=plain` completed successfully. The graph included `compileJava`, resource preparation, and `runServer`, and contained no `runClient` task. Dry run skipped every task and started no Minecraft process. The checkout `build` output remained absent. The wrapper is tracked without its executable bit, so the check did not change its file mode. The Gradle single use daemon stopped at command completion; the pre-existing shared Gradle caches remain protected.

No proxy, Forge backend, or Minecraft client was started during this preflight. The laptop client gate remains held while the owner uses a personal Minecraft client. Existing listeners on ports 25565 through 25568 were preserved; any later fixture must recheck and register distinct free ports before launch.

At 2026-09-25 21:37 UTC, the reobfuscated JAR build from checkout commit `e59d90c95375936349222ef7667510d029c628e4` completed successfully under Java 17.0.20.1 with `reobfJar`; its graph had no client task. The artifact SHA-256 is `90ff6e7f3c7ad243b119f320ac280eee64888bff5c076c50f28832e955be07ec`. Static inspection found no classes under Minecraft client or Blaze3D packages; `TabPlaceholderRenderer` sends a server-side tab-list packet and is not a client renderer. Forge installer 47.3.12 and MixinExtras Forge 0.5.3 were fetched into the registered candidates directory; their SHA-256 values match the earlier fixture records. The exact Forge installer completed `--installServer` successfully in both registered backend directories using Java 17.0.20.1. Neither server nor the proxy has been launched yet. Ports 25565 through 25568 and all pre-existing listeners remain untouched.

At 2026-09-25 21:54 UTC, the two registered Forge servers were started with the copied target JAR, PCF 1.3.1, and MixinExtras 0.5.3. Backend A reached `Done` at `backend-a/logs/latest.log:97`; backend B reached `Done` at `backend-b/logs/latest.log:97`. Both initialized SEF at line 99 and loaded MixinExtras 0.5.3 at line 53 and PCF at line 55. They ran on Java 17.0.20.1 with Forge 47.3.12 and Minecraft 1.20.1. EULA files were read back as `eula=true` immediately before launch. Backend A and B listened only on `127.0.0.1:26555` and `127.0.0.1:26556`. Each PCF config used `MODERN_DEFAULT`, debug logging, and the same disposable forwarding secret as Velocity; their config SHA-256 values were both `34db947fd58ff341a84f7208fe722f08cddbf912c1934106905db63b96975aba`. The secret itself is not recorded.

Velocity 4.2.0 build 30 ran on Java 25.0.4.1 with Ambassador 1.4.5 loaded. It reached `Done` at `proxy/logs/latest.log:10` and listened only on Tailscale `100.76.164.109:26557` at line 9. Its non-secret `velocity.toml` SHA-256 is `883975f4671fe62231056052a79f33efa20062fec05eadfdc9d0417edc0593c0`. bStats was explicitly disabled in the fixture config before this final startup. One earlier startup attempt exited before binding because sample forced-host mappings referenced removed template servers; the mappings were corrected locally before the successful start. No Minecraft client connected, so forwarding, signed chat, client commands, and backend transfer remain unverified.

The exact two Forge sessions and Velocity session were then stopped cleanly. Both Forge logs confirm that all worlds and dimensions were saved. Their owned process sessions exited with status 0, PIDs 4069048 and 4070004 no longer exist, and a final listener check found ports 26555 through 26557 closed. The pre-existing services and ports 25565 through 25568 were preserved. The registered `k` fixture files remain only for the pending laptop-client gate; no owned server, proxy, Minecraft client, or audio watcher remains running.

At 2026-09-25 21:56 UTC, the initially absent checkout `build` directory and both completed Forge installer log files were removed after their final consumers. The staged SEF copies in backend A and B still match the recorded artifact SHA-256. Their absence was verified. The registered candidate artifacts and installed fixture runtimes remain because the pending isolated laptop-client gate still consumes them.
